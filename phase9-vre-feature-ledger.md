# Phase 9 VRE Feature Ledger

## Purpose

This is the append-only VRE-side ledger for Phase 9 implementation.

It answers, at any point during Phase 9 implementation and afterward:

> Which VRE features were added, in which wave, in which files, and with
> which tests?

This file is updated in the same patch as every Phase 9 VRE code change.
It is never manually edited to make history prettier.

Companion files:

- **Spec-side status ledger**:
  `../vibe-science/blueprints/private/phase9-vre-autonomous-research-loop/16-implementation-status-ledger.md`
  tracks which spec contract clusters are `not-started`, `in-progress`,
  `implemented`, or `verified`.
- **Rotation index**: `phase9-vre-feature-ledger-index.md` (this directory)
  lists all ledger files (active + archived) by seq range and dates.
- **Machine surface inventory**:
  `phase9-vre-surface-index.json` is regenerated from the codebase and
  cross-checked against this ledger. Landed by Wave 0 `T0.4a-ter`.

## Update Rules

- Append new rows. Do NOT rewrite old rows.
- If a past row is wrong, append a correction row that references the old
  `seq` and explains the superseding state.
- Co-update the spec-side status ledger in the same patch.
- CI enforcement: `npm run check:phase9-ledger` (owned by Wave 0 `T0.4a-bis`
  and extended by `T0.4a-ter`) fails if this ledger is not updated in the
  same patch as a covered VRE code change, or if cross-check against the
  machine surface inventory raises `E_LEDGER_MISSING_SURFACE` /
  `E_LEDGER_ORPHAN_ROW` / `E_LEDGER_INDEX_INCONSISTENT`.
- Rotation: once the active ledger reaches `400` rows, the next pass that
  appends new VRE feature rows MUST prepare the successor file; rotation is
  mandatory no later than `500` rows. File size (`250 KB`) remains a
  secondary safety trigger if hit earlier, and operator request may trigger
  earlier rotation for readability. Successors continue as
  `phase9-vre-feature-ledger-02.md`, `-03.md`, and so on; rotation is atomic
  and monotonic `seq` is preserved across files.

## Row Contract

| Field | Meaning |
|---|---|
| `seq` | Monotonic row id. Starts at `000`. |
| `date` | Landing date (ISO-8601). |
| `wave` | Wave that owns the change: `pre-wave-0`, `0`, `1`, `2`, `3`, `4`, `4.5`, `5`, `6`. |
| `feature id` | Short stable identifier, e.g. `W0-CLI-PARSER`, `W2-OBJECTIVE-POINTER`. |
| `surface` | Human-readable name of the feature. |
| `paths` | Main VRE code paths added or changed. |
| `flags` | Feature flags involved (`VIBE_PHASE9_ENABLED`, etc.), or `none`. |
| `tests` | Concrete tests or acceptance artifacts proving the surface, or `TBD` if deferred. |
| `status` | `ready` (surface exists, no runtime features yet), `implemented` (code landed, tests partial), or `verified` (code landed + required tests green). |
| `notes` | Short note for drift, rollback, correction references, or follow-up context. |

## Ledger

| seq | date | wave | feature id | surface | paths | flags | tests | status | notes |
|---|---|---|---|---|---|---|---|---|---|
| 000 | 2026-04-21 | 0 | TRACKING-BOOTSTRAP | Phase 9 VRE feature-ledger infrastructure | `phase9-vre-feature-ledger.md`, `phase9-vre-feature-ledger-index.md` | none | none | implemented | Bootstrap. Created by Wave 0 `T0.1a` after explicit operator GO recorded as Round 15 in both review logs (`../vibe-science/blueprints/private/phase9-vre-autonomous-research-loop/12-spec-self-review-log.md`, `../vibe-science/blueprints/private/phase9-implementation-plan/11-plan-self-review-log.md`). These two files are the first Phase 9 artifacts in the VRE repo. No Phase 9 runtime features have landed yet. Wave 0 tasks `T0.2`, `T0.3`, `T0.4`, `T0.4a`, `T0.4a-bis`, `T0.4a-ter`, `T0.5`, `T0.6` will append their rows here as they land. |
| 001 | 2026-04-21 | 0 | W0-CI-LEDGER-CHECK | Phase 9 ledger CI enforcement runner | `package.json`, `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `environment/tests/ci/run-all.js`, `environment/tests/ci/validate-counts.js` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js`; `npm run check:phase9-ledger` | implemented | Wave 0 `T0.4a-bis`. Superseded in part by seq 002 correction: this row was initially marked `verified`, but the test suite covered only explicit mode and the checker silently skipped spec-ledger enforcement in discovered mode. See seq 002 (correction) and seq 003 (hardening). |
| 002 | 2026-04-21 | 0 | CORRECTION-001 | Ledger-row correction: downgrade seq 001 status and record partial-verify finding | `phase9-vre-feature-ledger.md` | none | none (correction row, no new tests) | implemented | Correction of seq 001. Root cause: `check-phase9-ledger.js` had `mustSeeSpecLedgerUpdate = mode === 'explicit'` on line 266, causing the spec-ledger enforcement to silently skip in discovered mode (local `npm run validate`). The test suite had 5 tests, all explicit-mode. Discovered mode was untested. The `verified` status on seq 001 was an over-claim. seq 003 lands the hardening fix. |
| 003 | 2026-04-21 | 0 | W0-CI-LEDGER-CHECK-HARDENING | Harden ledger CI enforcement to fire in discovered mode; add discovered-mode test coverage | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `phase9-vre-feature-ledger.md` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js` (7/7 pass, including 2 new discovered-mode tests); `npm run check:phase9-ledger` | implemented | Round 17. Removes the `mustSeeSpecLedgerUpdate = mode === 'explicit'` gating so spec-ledger enforcement fires unconditionally. Adds `options.discoveryOverride` test injection so discovered-mode fail-closed can be exercised without a live git workspace. Adds 2 new tests. Error message now includes triggering paths and mode for diagnostics. Superseded-in-part by seq 004: the hardening introduced a legitimate false-red for `npm run validate` because the spec ledger is gitignored in its host repo; seq 004 adds gitignore-aware handling. |
| 004 | 2026-04-21 | 0 | W0-CI-LEDGER-CHECK-GITIGNORE-AWARE | Make discovered-mode spec-ledger check gitignore-aware with loud diagnostic instead of false-red | `environment/tests/ci/check-phase9-ledger.js`, `phase9-vre-feature-ledger.md` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js` (7/7 pass); `npm run validate` (emits stderr diagnostic, exits 0); `npm run check:phase9-ledger -- --changed-file=... (explicit mode remains strict)` | verified | Round 17 refinement. The seq 003 hardening correctly removed the unconditional silent-skip but created a false-red for local `npm run validate`: the spec-side ledger lives under `blueprints/private/` which is gitignored in the vibe-science repo, so discovered mode (git diff + git status) cannot see its updates. Seq 004 adds `isSpecLedgerInGitignoredTree()` helper (walks up to find `.git`, calls `git check-ignore`). When discovered mode detects the spec ledger is in a gitignored tree, the check emits a loud stderr diagnostic explaining the limitation and skips only the spec-ledger requirement. Explicit mode (CI, or `--changed-file=...` args) remains strict. The diagnostic is NOT silent: operator sees it on every `npm run validate`. Closes the local UX false-red without weakening CI enforcement. |
| 005 | 2026-04-21 | 0 | LEDGER-HEADER-ROTATION-POLICY-SYNC | Align active VRE ledger header with the canonical 400-prepare / 500-rotate policy | `phase9-vre-feature-ledger.md` | none | none (document sync only) | implemented | Documentary correction. The active ledger header still said "exceeds 500 / 250 KB / operator request" while the canonical policy in `../vibe-science/blueprints/private/phase9-implementation-plan/13-implementation-tracking-discipline.md` had already been tightened to "prepare in the 400-500 row window, MUST rotate no later than 500". This row records the sync so the live VRE ledger and the frozen blueprint stop saying different things about rotation. No runtime surface changed. |
| 006 | 2026-04-21 | 0 | W0-SURFACE-INDEX-CROSSCHECK | Machine-generated Phase 9 surface inventory and CI cross-check against ledger + rotation index | `package.json`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-counts.js`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js environment/tests/ci/phase9-surface-index.test.js` (13/13 pass); `npm run build:surface-index`; `npm run validate`; `npm run check:phase9-ledger -- --changed-file=...` | verified | Wave 0 `T0.4a-ter`. Lands the machine-generated `phase9-vre-surface-index.json`, the reviewed generator script, and the three drift checks promised by the plan: `E_LEDGER_MISSING_SURFACE`, `E_LEDGER_ORPHAN_ROW`, and `E_LEDGER_INDEX_INCONSISTENT`. `check-phase9-ledger` now cross-checks the live codebase inventory against the persisted inventory file and against inventory-eligible ledger rows; the rotation index is also validated for one active row, archived closed dates, and seq-range continuity. This closes the remaining silent-drift gap after `T0.4a-bis`: the human ledger and the codebase can no longer diverge quietly. Partially refined by seq 007 (Round 20) which closes the residual inventory-absence loophole observed during the Round 19 adversarial review. |
| 007 | 2026-04-21 | 0 | W0-CI-LEDGER-CHECK-INVENTORY-REQUIRED | Round 20 closure of Round 19 adversarial-review residuals: new `E_LEDGER_SURFACE_INDEX_MISSING` drift code plus 3 new regression tests | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `phase9-vre-feature-ledger.md` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js environment/tests/ci/phase9-surface-index.test.js` (16/16 pass); manual attack-retest `rm phase9-vre-surface-index.json && node environment/tests/ci/check-phase9-ledger.js` now exits 1 with the new code (was exit 0 green before Round 20); `npm run validate` green | verified | Round 20. Closes the Round 19 residual where deleting `phase9-vre-surface-index.json` silently disabled the entire cross-check block because the block was gated by `pathExists(surfaceIndex)`. The fix parses `eligibleLedgerRows` once and raises `E_LEDGER_SURFACE_INDEX_MISSING` when there is at least one eligible row but no inventory file. Also adds regression tests for the two rotation-index violations that existed in code but had no direct test (`seq-range gap`, `archived row missing closed date`). The path-overlap lenience in `surfaceMatchesLedgerRow` (Round 19 residual #2) is DEFERRED as a known follow-up: tightening it requires adding a `parent seq` field to the ledger schema and is out of scope for a hot-fix. The mid-dev bugs Codex caught during Round 19 (`pathExists-without-await`, `repoRoot-global`) never reached committed state and therefore do not require separate correction rows. Round 20 does NOT introduce new spec P0/P1. |
| 008 | 2026-04-22 | 0 | LEDGER-DOC-SYNC-ROUND-21 | Align live VRE ledger docs with the already-landed rotation and surface-index state | `phase9-vre-feature-ledger.md`, `phase9-vre-feature-ledger-index.md` | none | none (document sync only) | implemented | Documentary sync. `phase9-vre-feature-ledger.md` no longer says the machine surface inventory is "to land" after `T0.4a-ter` already landed, and `phase9-vre-feature-ledger-index.md` now uses the same 400-prepare / 500-mandatory rotation rule as the canonical tracking-discipline blueprint. No runtime surface changed. |
| 009 | 2026-04-22 | 0 | W0-PROJECTION-COUNT-DRIFT-FIX | Resolve the kernel-bridge 9-vs-8 projection drift by pinning the contract to eight and adding fixture-driven cross-repo drift tests | `environment/lib/kernel-bridge.js`, `environment/tests/lib/kernel-bridge.test.js`, `environment/tests/integration/kernel-bridge.test.js`, `../vibe-science/tests/fixtures/kernel-bridge-projection-count.fixture.json`, `../vibe-science/tests/kernel-bridge-projection-count.test.mjs` | none | `node --test environment/tests/lib/kernel-bridge.test.js environment/tests/integration/kernel-bridge.test.js`; `node --test tests/kernel-bridge-projection-count.test.mjs` | verified | Wave 0 `T0.2`. Code review found no concrete ninth projection in the sibling kernel, so this follows the plan's default choice: fix the declared count to eight instead of inventing a ninth projection. The new fixture-driven test fails if the VRE typed-duck list, the live sibling kernel exports, or the declared count drift apart again. |
| 010 | 2026-04-22 | 0 | W0-CI-LEDGER-CHECK-NON-SURFACE-REFINEMENT | Refine ledger cross-check so non-surface implementation rows are not misclassified as orphaned machine surfaces | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `phase9-vre-feature-ledger.md` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js`; `npm run validate`; explicit `npm run check:phase9-ledger -- --changed-file=...` | implemented | Round 21 refinement after read-after-write verification. `seq 009` landed a real bridge-contract fix, but `npm run validate` exposed that the ledger checker still treated every implemented/verified row touching `environment/**` as inventory-backed. This refinement used a `surfaceBackedLedgerRows` pre-filter to skip the orphan check for rows that did not match any surface. Superseded in part by seq 011 (Round 22 adversarial-review closure): the pre-filter was found to silently skip `E_LEDGER_ORPHAN_ROW` for genuinely broken rows (covered surface paths, no live or persisted match) — exactly the scenario the check was designed to catch. seq 011 lands the principled fix by tightening `isInventoryTrackablePath` instead of filtering rows post-hoc. |
| 011 | 2026-04-22 | 0 | W0-CI-LEDGER-CHECK-TIGHTEN-TRACKABLE-PATH | Round 22 closure of seq 010 silent-skip: tighten `isInventoryTrackablePath` to align with `isCoveredVrePath` and revert the `surfaceBackedLedgerRows` filter | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `phase9-vre-feature-ledger.md` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js environment/tests/ci/phase9-surface-index.test.js` (18/18 pass; 1 new Round 22 regression test); attack-retest on live ledger (`append fake orphan row with path environment/orchestrator/should-not-exist.js && node environment/tests/ci/check-phase9-ledger.js --changed-file=phase9-vre-feature-ledger.md`) exits 1 with `E_LEDGER_ORPHAN_ROW seq 099 (W0-SIMULATED-ORPHAN) has no matching live surface` (was exit 0 green under seq 010 pre-filter); `npm run validate` green with the gitignored-spec-ledger diagnostic still emitted; explicit `npm run check:phase9-ledger -- --changed-file=...` green | verified | Round 22. Closes the silent-skip Codex introduced in seq 010. The seq 010 `surfaceBackedLedgerRows` pre-filter was semantically wrong: by excluding rows that matched NEITHER live nor persisted inventory, it also excluded the exact "code never landed" drift case the orphan check was designed to catch. Round 22 reverts that filter and fixes the root cause instead: `isInventoryTrackablePath` was too loose (accepted any `environment/**` path). It is now aligned with `isCoveredVrePath` (`bin/vre`, `environment/control/`, `environment/objectives/`, `environment/orchestrator/`, `environment/scheduler/`, `environment/schemas/`, `environment/tests/ci/`, `environment/tests/cli/`, `environment/tests/schemas/`, `environment/tests/fixtures/phase9/`, plus `package.json` and the surface-index file). Rows that touch only library code (e.g. `environment/lib/kernel-bridge.js` in T0.2 seq 009) become naturally ineligible without a post-hoc skip, and the strict orphan rule holds for every eligible row on a covered surface path. Added 1 new regression test locking in the "covered path, no match → ORPHAN FIRES" contract. No new spec P0/P1 introduced. |
| 012 | 2026-04-22 | 0 | W0-CI-LEDGER-CHECK-STRICT-LIVE-SURFACE-MATCH | Tighten live-surface matching so feature-bearing CLI surfaces cannot be masked by shared-path overlap | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js environment/tests/ci/phase9-surface-index.test.js` (19/19 pass) | verified | Round 23 drift closure before `T0.3`. The Round 20 deferred lenience became a real risk once multiple CLI surfaces started to share `bin/vre`: one ledger row could otherwise satisfy several live surfaces by path overlap alone. Live surface -> ledger matching is now featureId-authoritative when the live surface has a non-null `featureId`; orphan-side matching keeps the path-overlap fallback so correction/hardening rows that annotate an existing surface do not become false orphan rows. This closes the last known false-green path before landing the CLI stubs. |
| 013 | 2026-04-22 | 0 | W0-CLI-CAPABILITIES-JSON | Phase 9 CLI stub: `capabilities --json` | `bin/vre` | none | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real capability-handshake payload lands in Wave 1. |
| 014 | 2026-04-22 | 0 | W0-CLI-CAPABILITIES-DOCTOR | Phase 9 CLI stub: `capabilities doctor` | `bin/vre` | none | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real readiness contract lands in Wave 1. |
| 015 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-START | Phase 9 CLI stub: `objective start` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real objective creation lands in Wave 2. |
| 016 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-STATUS | Phase 9 CLI stub: `objective status` | `bin/vre` | none | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real morning summary lands in Wave 2. |
| 017 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-DOCTOR | Phase 9 CLI stub: `objective doctor` | `bin/vre` | none | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real doctor contract lands across Wave 2 and Wave 4. |
| 018 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-STOP | Phase 9 CLI stub: `objective stop` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real stop semantics land in Wave 2. |
| 019 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-PAUSE | Phase 9 CLI stub: `objective pause` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real pause semantics land in Wave 2. |
| 020 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-RESUME | Phase 9 CLI stub: `objective resume` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real resume/repair semantics land in Wave 2-4. |
| 021 | 2026-04-22 | 0 | W0-CLI-RUN-ANALYSIS | Phase 9 CLI stub: `run-analysis` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real sanctioned analysis execution lands in Wave 3. |
| 022 | 2026-04-22 | 0 | W0-CLI-RESEARCH-LOOP | Phase 9 CLI stub: `research-loop` (including parsed `--resume`) | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real unattended loop lands in Wave 4. |
| 023 | 2026-04-22 | 0 | W0-CLI-SCHEDULER-INSTALL | Phase 9 CLI stub: `scheduler install` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real Windows scheduler installation lands in Wave 4. |
| 024 | 2026-04-22 | 0 | W0-CLI-SCHEDULER-STATUS | Phase 9 CLI stub: `scheduler status` | `bin/vre` | none | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real support-mode/status semantics land in Wave 4. |
| 025 | 2026-04-22 | 0 | W0-CLI-SCHEDULER-DOCTOR | Phase 9 CLI stub: `scheduler doctor` | `bin/vre` | none | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real readiness contract lands in Wave 4. |
| 026 | 2026-04-22 | 0 | W0-CLI-SCHEDULER-REMOVE | Phase 9 CLI stub: `scheduler remove` | `bin/vre` | `VIBE_PHASE9_ENABLED` (declared in stub metadata; enforced when implementation lands) | `node --test environment/tests/cli/*.test.js`; `npm run build:surface-index` | verified | Wave 0 `T0.3`. Structured stub only; real scheduler removal lands in Wave 4. |
| 027 | 2026-04-22 | 0 | W0-TIME-PROVIDER-SEAM | Reviewed deterministic time seam with cross-process test-clock offset support | `environment/control/time-provider.js`, `environment/tests/control/time-provider.test.js` | none | `node --test environment/tests/control/time-provider.test.js`; `node --test environment/tests/control/*.test.js` | verified | Wave 0 `T0.4`. Lands the reviewed `TimeProvider` surface required by Scenario B3 before runtime waves start. The seam exposes `nowIso()`, `nowMs()`, and `statMtimeMs()` keyed by project root, and honors `.vibe-science-environment/control/test-clock-offset.json` across subprocess boundaries. |
| 028 | 2026-04-22 | 0 | W0-MEMORY-API-ALLOWLIST | Reviewed checked-in authority for memory API exposure in the future capability handshake | `environment/control/approved-memory-apis.json`, `environment/tests/control/approved-memory-apis.test.js` | none | `node --test environment/tests/control/approved-memory-apis.test.js`; `node --test environment/tests/control/*.test.js` | verified | Wave 0 `T0.4`. The allowlist is now the only reviewed authority for memory API exposure. Tests fail both when an allowlist row points to a missing module/export and when an on-disk memory export exists but is absent from the reviewed allowlist. |
| 029 | 2026-04-22 | 0 | W0-SURFACE-INDEX-T0.4-COVERAGE | Extend the machine surface inventory so T0.4 seams are tracked surfaces instead of orphan rows | `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-counts.js`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/ci/phase9-surface-index.test.js`; `npm run build:surface-index`; `npm run validate` | verified | Round 24 tracking closure required by T0.4. Without this refinement the new `environment/control/` seams would have become honest `E_LEDGER_ORPHAN_ROW` failures because the machine inventory previously knew only CLI + CI surfaces. Persisted inventory is now 18 total surfaces: 14 CLI + 2 CI tracking + 2 T0.4 control seams. |
| 030 | 2026-04-22 | 0 | W0-CI-LEDGER-CHECK-CI-SIBLING-ABSENT | Make `check-phase9-ledger` CI-runnable on a vibe-research-environment-only GitHub Actions checkout where the sibling vibe-science repo is absent | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js`, `phase9-vre-feature-ledger.md` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js` (19/19 pass; 2 new Round 25 regression tests); `npm run validate` (green with two expected stderr diagnostics); live CI simulation (`mv vibe-science vibe-science.tempmoved && node environment/tests/ci/check-phase9-ledger.js`) exits 0 with both "review log(s) absent" and "does not exist in the sibling workspace" diagnostics (was exit 1 `ENOENT` before Round 25); `npm test` 552/552 pass + 5 skipped | verified | Round 25 CI closure. GitHub Actions `CI / validate-and-test` failed on commit `2af0ae5` with `ENOENT: no such file or directory, open '/home/runner/work/vibe-research-environment/vibe-science/blueprints/private/phase9-vre-autonomous-research-loop/12-spec-self-review-log.md'`. Root cause: the checker read both Phase 9 review logs unconditionally to verify the T0.1a GO entry, but in CI runs of vibe-research-environment alone the sibling vibe-science repo is not checked out (the Phase 9 logs live under `vibe-science/blueprints/private/` and are gitignored there by design). A second path surfaced after the first fix: `isSpecLedgerInGitignoredTree()` walks up looking for `.git`, finds the VRE repo, and `git check-ignore` against an out-of-repo path returns non-zero, so the spec-ledger discovered-mode skip was not triggered in CI either. Round 25 fixes both: (1) the GO check now reads the review logs only when both files exist, otherwise it emits a loud stderr diagnostic and skips the assertion; (2) the spec-ledger discovered-mode branch now checks `pathExists(PATHS.specLedger)` first and treats "absent entirely" as its own diagnostic case, before falling back to the gitignored-in-sibling-repo branch from Round 17. Strict enforcement still applies in explicit mode (`--changed-file=...` or a combined-workspace CI where both repos are checked out). No runtime surface changed; the fix is purely about making the CI guardrail runnable against the GitHub Actions checkout topology. |
| 031 | 2026-04-22 | 0 | W0-SCHEMA-OBJECTIVE-ROOTS | Land the root Phase 9 objective schemas and canonical fixtures for runtime budget, objective record, and active objective pointer | `environment/schemas/phase9-runtime-budget.schema.json`, `environment/schemas/phase9-objective.schema.json`, `environment/schemas/phase9-active-objective-pointer.schema.json`, `environment/tests/fixtures/phase9/objective/valid-active.json`, `environment/tests/fixtures/phase9/objective/valid-blocked.json`, `environment/tests/fixtures/phase9/objective/invalid-missing-budget.json`, `environment/tests/fixtures/phase9/objective/invalid-missing-reasoning-mode.json`, `environment/tests/fixtures/phase9/active-objective-pointer/valid-active.json`, `environment/tests/fixtures/phase9/active-objective-pointer/invalid-missing-wake-lease.json`, `environment/tests/schemas/phase9-runtime-budget.schema.test.js`, `environment/tests/schemas/phase9-objective.schema.test.js`, `environment/tests/schemas/phase9-active-objective-pointer.schema.test.js` | none | `node --test environment/tests/schemas/phase9-*.test.js` (30/30 pass); `npm run build:surface-index` | verified | Wave 0 `T0.4a`. `phase9.objective.v1` now requires `reasoningMode` and pins v1 to `rule-only`; the objective fixture set now matches the file 14 canonical names instead of relying on a partial subset. |
| 032 | 2026-04-22 | 0 | W0-SCHEMA-RECONCILIATION-FIELDS | Land the reconciliation JSON/JSONL schemas and canonical fixtures for `recordSeq`, `reasoningMode`, wake-lease mirror state, and state-repair semantics | `environment/schemas/phase9-objective-event.schema.json`, `environment/schemas/phase9-handoff.schema.json`, `environment/schemas/phase9-resume-snapshot.schema.json`, `environment/schemas/phase9-lane-run-record.schema.json`, `environment/tests/fixtures/phase9/objective-event/valid-loop-iteration.json`, `environment/tests/fixtures/phase9/objective-event/valid-state-repair.json`, `environment/tests/fixtures/phase9/objective-event/valid-semantic-drift-detected.json`, `environment/tests/fixtures/phase9/objective-event/invalid-missing-record-seq.json`, `environment/tests/fixtures/phase9/objective-event/invalid-state-repair-missing-repaired-layer.json`, `environment/tests/fixtures/phase9/handoff/valid-basic.json`, `environment/tests/fixtures/phase9/handoff/valid.jsonl`, `environment/tests/fixtures/phase9/handoff/invalid-missing-record-seq.json`, `environment/tests/fixtures/phase9/handoff/invalid-no-artifacts.jsonl`, `environment/tests/fixtures/phase9/resume-snapshot/valid-mid-loop.json`, `environment/tests/fixtures/phase9/resume-snapshot/valid-pre-stop.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-missing-budget.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-missing-reasoning-mode.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-reasoning-mode-diverged.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-stale-fingerprint.json`, `environment/tests/fixtures/phase9/lane-run-record/valid-complete.json`, `environment/tests/fixtures/phase9/lane-run-record/valid-interrupted.json`, `environment/tests/fixtures/phase9/lane-run-record/invalid-missing-record-seq.json`, `environment/tests/schemas/phase9-objective-event.schema.test.js`, `environment/tests/schemas/phase9-handoff.schema.test.js`, `environment/tests/schemas/phase9-resume-snapshot.schema.test.js`, `environment/tests/schemas/phase9-lane-run-record.schema.test.js` | none | `node --test environment/tests/schemas/phase9-*.test.js` (30/30 pass); `npm run build:surface-index` | verified | Wave 0 `T0.4a`. Adds required monotonic `recordSeq` to every resume-reconciliation JSONL line schema landed so far, pins `phase9.resume-snapshot.v1.reasoningMode` to `rule-only`, and closes the fixture-layout drift against file 14 before Wave 4 runtime work. |
| 033 | 2026-04-22 | 0 | W0-RUNTIME-SPAWN-FIXTURES | Land the reviewed subprocess envelope schema and runtime-spawn invalid fixtures for env-leak and cwd-escape failures | `environment/schemas/phase9-role-envelope.schema.json`, `environment/tests/fixtures/phase9/role-envelope/valid-subprocess.json`, `environment/tests/fixtures/phase9/role-envelope/invalid-missing-role.json`, `environment/tests/fixtures/phase9/role-envelope/invalid-missing-session-isolation.json`, `environment/tests/fixtures/phase9/runtime-spawn/invalid-env-leak.json`, `environment/tests/fixtures/phase9/runtime-spawn/invalid-cwd-escape.json`, `environment/tests/schemas/phase9-role-envelope.schema.test.js` | none | `node --test environment/tests/schemas/phase9-*.test.js` (30/30 pass) | verified | Wave 0 `T0.4a`. The schema layer now carries the cold-child transport surface and the runtime-spawn fixture set promised by file 07/file 14; tests fail closed on `E_ENV_LEAK` and `E_CWD_ESCAPE`, and the canonical `invalid-missing-role.json` fixture now exists instead of staying phantom. |
| 034 | 2026-04-22 | 0 | W0-CLI-OBJECTIVE-START-REASONING-GATE | Reject unsupported Phase 9 v1 reasoning-mode selection at the CLI boundary | `bin/vre`, `environment/tests/cli/bin-vre-phase9-stubs.test.js` | none | `node --test environment/tests/cli/*.test.js` (22/22 pass) | verified | Wave 0 `T0.4a`. `objective start --reasoning-mode reviewed-api` now fails with `E_REASONING_MODE_UNSUPPORTED` and `supported=[\"rule-only\"]`, so the v1 reasoning boundary is enforced at objective creation instead of being deferred to later runtime code. |
| 035 | 2026-04-22 | 0 | W0-SURFACE-INDEX-T0.4A-COVERAGE | Extend the machine surface inventory and runtime-contract validators so the newly landed Phase 9 schemas are tracked surfaces instead of untracked files | `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-counts.js`, `environment/tests/ci/validate-runtime-contracts.js`, `phase9-vre-surface-index.json` | none | `npm run build:surface-index` (26 total surfaces: 14 CLI + 2 CI + 2 control seams + 8 Phase 9 schemas); `node --test environment/tests/ci/check-phase9-ledger.test.js environment/tests/ci/phase9-surface-index.test.js` (21/21 pass) | verified | Round 26 tracking closure for `T0.4a`. Without this extension the 8 new schema files would stay off the machine inventory and `npm run validate` would correctly fail with `E_LEDGER_MISSING_SURFACE`. The inventory and active-runtime-contract validator now know about all landed T0.4a schema surfaces. |
| 036 | 2026-04-22 | 0 | W0-PHASE9-TEST-ENTRYPOINT | Named Phase 9 test entrypoints exist in both repos while the repo-wide suites remain comprehensive | `package.json`, `../vibe-science/package.json` | none | `npm run test:phase9` in `vibe-research-environment` (99 pass, 0 fail, 1 skipped); `npm run test:phase9` in `vibe-science` (2/2 pass); `npm test` in `vibe-science` (green: 171 E2E + 335 Phase 8 + 2 Phase 9 pass) | verified | Wave 0 `T0.5`. Creates a named Phase 9 test entrypoint in both repos instead of leaving Phase 9 coverage implicit inside larger suites. The companion cross-repo false-red closure is documented in Round 27: `vibe-science/tests/delivery-discipline-skill.test.mjs` now parses both LF and CRLF frontmatter so Windows checkouts do not fail `npm test` for non-semantic line-ending reasons. |
| 037 | 2026-04-22 | 0 | W0-PHASE9-TEST-SCAFFOLDING-VALIDATORS | Pin the Phase 9 test file set and require the named Phase 9 entrypoint in CI | `.github/workflows/ci.yml`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-ci-workflow.js`, `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js environment/tests/ci/phase9-surface-index.test.js` (22/22 pass, including workflow-coverage regression); `npm run check` (green) | verified | Wave 0 `T0.5`. `validate-runtime-contracts.js` now pins the 15-file Phase 9 test set behind `package.json -> test:phase9`; `validate-ci-workflow.js` now requires an explicit `npm run test:phase9` step in at least one compliant workflow; and `.github/workflows/ci.yml` is now a covered VRE path so workflow drift cannot bypass ledger discipline silently. |
| 038 | 2026-04-22 | 0 | W0-SURFACE-INDEX-T0.5-COVERAGE | Extend the machine surface inventory so the named Phase 9 test entrypoint is machine-tracked | `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `phase9-vre-surface-index.json` | none | `npm run build:surface-index` (27 total surfaces: 14 CLI + 3 test-entrypoint + 2 control seams + 8 schemas); `node --test environment/tests/ci/phase9-surface-index.test.js` | verified | Round 27 tracking closure for `T0.5`. Without this extension the new `test:phase9` entrypoint would exist in code and CI but remain invisible to the machine inventory. The persisted surface index now includes `test:phase9` as `W0-PHASE9-TEST-ENTRYPOINT`. |
| 039 | 2026-04-22 | 0 | W0-GUARDRAIL-FUTURE-PLUGIN-PATHS | Pre-register future Phase 9 plugin-script guardrail paths against agent self-edit in production | `../vibe-science/plugin/scripts/pre-tool-use.js`, `../vibe-science/.claude/settings.json`, `../vibe-science/tests/governance-hooks.test.mjs` | none | `node --test tests/governance-hooks.test.mjs` (162/162 pass) | verified | Wave 0 `T0.6`. Adds deny coverage for `plugin/scripts/handshake-inject.js`, `plugin/scripts/objective-loader.js`, `plugin/scripts/loop-wake.js`, and `plugin/scripts/r2-bridge-writer.js` in both the runtime guardrail (`pre-tool-use.js`) and the independent settings deny-list. Tests assert direct `Write` denials for each future path. |
| 040 | 2026-04-22 | 0 | W0-GUARDRAIL-VRE-RUNTIME-PATH-PREREG | Pre-register the future Phase 9 autonomy runtime path as protected across sibling-repo writes | `../vibe-science/plugin/scripts/pre-tool-use.js`, `../vibe-science/.claude/settings.json`, `../vibe-science/tests/governance-hooks.test.mjs` | none | `node --test tests/governance-hooks.test.mjs` (162/162 pass) | verified | Wave 0 `T0.6`. Adds deny coverage for `../vibe-research-environment/environment/orchestrator/autonomy-runtime.js` so future Phase 9 runtime/autonomy code cannot be agent-edited through `Write` or Bash mutation paths before the reviewed surfaces land. Tests cover both sibling-style direct `Write` and Bash `rsync` denial. |
| 041 | 2026-04-22 | 0 | W0-CI-LEDGER-CHECK-DISPATCHER-ONLY-CLOSURE | Close the last known post-Wave-0 `bin/vre`-only orphan-match false-green for NEW ledger rows | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js` (22/22 pass); `npm run validate`; live fixture attack with a post-Wave-0 fake dispatcher-only row now raises `E_LEDGER_ORPHAN_ROW` | verified | Round 29 drift closure before Wave 1 runtime start. The remaining shared-path lenience on orphan-side matching is now narrowed for the real risk surface: `bin/vre` is a multiplexed dispatcher shared by many CLI surfaces, so a NEW dispatcher-only row with a fresh non-matching `feature id` can no longer masquerade as "matched" by path overlap alone. Legacy Wave 0 dispatcher-only hardening rows (for example `seq 034`) are grandfathered to avoid rewriting history; rows appended from `seq >= 041` must match a live CLI surface by `feature id` when `bin/vre` is their only trackable path. Superseded-in-part by seq 042: Round 29 left an adjacent variant open — a row with `bin/vre` PLUS a fake non-existent VRE-local path passed the orphan check via dispatcher path overlap. Round 30 closes that path via a new drift code `E_LEDGER_PHANTOM_PATH`. |
| 042 | 2026-04-22 | 0 | W0-CI-LEDGER-CHECK-PHANTOM-PATH | Close the mixed-path residual found during the Round 29 adversarial review by requiring every VRE-local path declared by an inventory-eligible row to exist on disk | `environment/tests/ci/check-phase9-ledger.js`, `environment/tests/ci/check-phase9-ledger.test.js` | none | `node --test environment/tests/ci/check-phase9-ledger.test.js` (24/24 pass; 2 new Round 30 regression tests: phantom-path firing, sibling-path skip); `npm run test:phase9` (104 pass, 1 skipped); `npm run check` green; live attack on the live ledger (`seq 099` with `paths=[bin/vre, environment/orchestrator/never-landed.js]`) now raises `E_LEDGER_PHANTOM_PATH seq 099 (W1-FAKE-MIXED) declares VRE-local path(s) that do not exist on disk: environment/orchestrator/never-landed.js` (was exit 0 silent-skip under Round 29 alone) | verified | Round 30 adversarial closure of the Round 29 residual. After Round 29 tightened the orphan check for pure dispatcher-only rows, an adversarial retest found the adjacent mixed-path case still silently passed because `bin/vre` path overlap with live CLI surfaces masked the fact that the row's OTHER declared path did not exist on disk. Round 30 adds `E_LEDGER_PHANTOM_PATH`: every VRE-local path (not starting with `../`) declared by an inventory-eligible row must resolve to an existing file. Sibling paths (`../vibe-science/...`) are skipped because the sibling repo may legitimately be absent in CI. All 42 existing eligible rows pass the new check — verified with a pre-edit sweep that enumerated every row path. The check is orthogonal to the orphan check: a row can pass orphan (featureId or path overlap) AND still fail phantom-path if it claims a non-existent VRE-local file. Together with seq 041 this closes the last known dispatcher-related false-green classes before Wave 1 runtime work begins. |
| 043 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-AGGREGATOR | Land the VRE-side capability aggregator that emits `phase9.capability-handshake.v1` from live repo state | `environment/control/capability-handshake.js`, `environment/lib/kernel-bridge.js`, `environment/tests/control/capability-handshake.test.js` | none | `node --test environment/tests/control/capability-handshake.test.js` (5/5 pass, including full live payload, no-kernel degraded, missing-VRE root, objective-pointer read, and reviewed-memory-API exclusion); `npm run test:phase9` (122 pass, 1 skipped) | verified | Wave 1 `T1.1`. This is the VRE-only aggregator surface before CLI/artifact wiring in `T1.2`. Same-day adversarial review found a real missing-VRE-path crash: the generator tried to load `phase9-capability-handshake.schema.json` from the absent target root. Closed before first ledger append by falling back to the host repo schema root and emitting a concrete `VRE_MISSING:` degraded reason instead of crashing. |
| 044 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-SCHEMA | Land the Phase 9 capability-handshake schema and canonical fixture set | `environment/schemas/phase9-capability-handshake.schema.json`, `environment/tests/fixtures/phase9/capability-handshake/`, `environment/tests/schemas/phase9-capability-handshake.schema.test.js` | none | `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass) | verified | Wave 1 `T1.1`. The fixture set now covers full live payload, degraded no-kernel, missing-VRE path, projection-probe shape, invalid missing `operatorSurface`, invalid missing `missingSurfaces`, invalid missing `objective`, invalid missing `memoryApis`, and synthetic invalid cases for missing ontology arrays. |
| 045 | 2026-04-22 | 1 | W1-SURFACE-INDEX-T1.1-COVERAGE | Extend Phase 9 machine tracking so the new handshake control surface, schema, and test pinning are inventory-visible and validator-pinned | `package.json`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `phase9-vre-surface-index.json` | none | `npm run build:surface-index` (29 total surfaces: 14 CLI + 3 test-entrypoint + 3 control/orchestrator + 9 schema); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass); `npm run test:phase9` (122 pass, 1 skipped); `npm run validate` (green after seq 043-045) | verified | Round 31 tracking closure for Wave 1 `T1.1`. Without this refinement the new handshake module and handshake schema would land in code but remain partially invisible to the machine inventory and Phase 9 runtime-contract pinning. |
| 046 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-AUTHORITY-HARDENING | Make the Wave 1 handshake truthful about what is executable now and which reviewed ontology bundles are actually available | `environment/control/capability-handshake.js`, `environment/tests/control/capability-handshake.test.js` | none | `node --test environment/tests/control/capability-handshake.test.js` (6/6 pass, including reviewed-source degraded ontology visibility); `npm run test:phase9` (123 pass, 0 fail, 1 skipped); `npm run validate` | verified | Round 32 adversarial closure on top of seq `043-045`. `vre.executableCommands` now derives only from the real `bin/vre` dispatch table (`flow-status`, `orchestrator-status`, `sync-memory`) while future/operator-only Phase 9 commands stay under `vre.operatorSurface`. Reviewed connectors, automations, and domain packs are now enumerated only from the authoritative reviewed manifests/loaders and degrade honestly when the corresponding reviewed core bundles are absent. `active-objective path` was also removed from `missingSurfaces`: an absent active pointer is a runtime state, not a missing implementation surface. |
| 047 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-FIXTURE-SYNC | Sync the canonical missing-VRE handshake fixture with the real degraded runtime branch so schema fixtures and live generation stay aligned | `environment/schemas/phase9-capability-handshake.schema.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-missing-vre-kernel.json`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/schemas/phase9-capability-handshake.schema.test.js` | none | `node --test environment/tests/control/capability-handshake.test.js` (6/6 pass); `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass) | verified | Round 32 closure of the second post-Round-31 P1. The canonical `valid-missing-vre-kernel.json` fixture now matches the real generator branch: `vrePresent=false`, `vrePath=null`, eight unavailable kernel probes, empty `missingSurfaces`, and a concrete `VRE_MISSING:` degraded reason. The row anchors that fixture sync to the live handshake schema surface, so the ledger records a real reviewed surface plus its canonical evidence instead of a test-only orphan row. |
| 048 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-FIXTURE-TRUTHFULNESS | Sync the remaining canonical handshake fixtures with the truthful T1.1 runtime contract after the Round 32 authority hardening | `environment/schemas/phase9-capability-handshake.schema.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/schemas/phase9-capability-handshake.schema.test.js` | none | `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass); `npm run test:phase9` (123 pass, 0 fail, 1 skipped); `npm run validate` | verified | Round 33 same-day fixture drift closure after the sub-agent review of Round 32. `valid-full.json` and `valid-degraded-no-kernel.json` no longer pretend that future/operator-only Phase 9 commands are executable now, and the degraded no-kernel branch no longer hides the reviewed connector/automation/domain-pack ontology behind empty arrays. The fixtures remain schema-valid, but now they are also representative of the post-Round-32 T1.1 runtime contract instead of silently preserving the pre-hardening behavior. |
| 049 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-FIXTURE-DEGRADED-REASON-SYNC | Sync the `degradedReasons` array in both canonical handshake fixtures with the real generator output after Round 33 so the fixtures also reproduce the kernel-debt and kernel-mode-prefix invariants of the live aggregator | `environment/schemas/phase9-capability-handshake.schema.json`, `environment/control/capability-handshake.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json` | none | `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass); `node --test environment/tests/control/capability-handshake.test.js` (6/6 pass); `npm run test:phase9` (123 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); runtime diff against live generator (full-mode with `FAKE_KERNEL_ROOT` and degraded-no-kernel with `kernelRoot=null`) now shows zero "in-fixture-not-runtime" items for the degradedReasons array (modulo memory/STALE scenario differences which are state-dependent, not fixture-invariant) | verified | Round 34 adversarial closure on top of Round 33. Round 33 synced the `vre.executableCommands` and reviewed-ontology visibility branches of the two fixtures but left three truthfulness residuals in their `degradedReasons` arrays: (1) `valid-full.json` was missing the unconditional `kernel unresolvedR2Count is currently derived from listUnresolvedClaims until a dedicated R2 projection lands` message that the live aggregator always emits in full mode; (2) `valid-degraded-no-kernel.json` was missing the complementary `kernel unresolvedR2Count is unavailable because listUnresolvedClaims is not currently available` message that the live aggregator emits whenever `listUnresolvedClaims` is not an array; (3) `valid-degraded-no-kernel.json` encoded `"kernel degraded: kernel root not found"` even though `kernel.mode` is already `"missing"` and the aggregator derives the prefix as `\`kernel ${mode}: ${unreachableReason}\`` so the real runtime would emit `"kernel missing: ..."`. All three residuals closed by appending the two R2-debt messages and rewriting the kernel-mode-prefix to `"kernel missing: kernel root not found"`. Fixtures stay schema-valid and now also reproduce the kernel-debt and kernel-mode-prefix invariants of the live aggregator, not just its shape. |
| 050 | 2026-04-22 | 1 | W1-CLI-CAPABILITIES-JSON-LIVE | Upgrade `node bin/vre capabilities --json` from a Wave 0 stub into the real Phase 9 handshake producer and make the CLI the source of truth for the handshake artifact | `bin/vre`, `environment/control/capability-handshake.js`, `environment/tests/cli/bin-vre-phase9-stubs.test.js`, `environment/tests/control/capability-handshake.test.js` | none | `node --test environment/tests/cli/bin-vre-phase9-stubs.test.js` (7/7 pass); `node --test environment/tests/control/capability-handshake.test.js` (7/7 pass); `node --test environment/tests/cli/*.test.js` (25/25 pass); `npm run test:phase9` (127 pass, 0 fail, 1 skipped); `npm run validate` | verified | Wave 1 `T1.2`. `capabilities --json` now emits JSON only, rewrites `.vibe-science-environment/control/capability-handshake.json` atomically on every successful call, and passes the same payload bytes to stdout and artifact. The generator now receives reviewed CLI metadata directly from the caller so the real command path does not re-import `bin/vre` recursively while it is executing. The handshake truthfully reports `capabilities --json` as executable and no longer lists it under `missingSurfaces`. |
| 051 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-FIXTURE-CLI-TRUTH-SYNC | Sync the canonical handshake fixtures and fixture-truthfulness tests with the post-T1.2 CLI reality | `environment/control/capability-handshake.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json` | none | `node --test environment/tests/control/capability-handshake.test.js` (7/7 pass); `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass); `npm run test:phase9` (127 pass, 0 fail, 1 skipped); `npm run validate` | verified | Round 35 same-pass truthfulness closure for `T1.2`. The canonical full + degraded-no-kernel fixtures now reflect that `capabilities --json` is a real executable command, no longer claim it as missing, and the degraded no-kernel fixture also carries the current reviewed no-reader message (`kernel missing: CLI default: no reader provided`) plus the reviewed ontology degraded reasons that are invariant for the no-kernel branch. A dedicated control test now pins those invariants so this CLI/fixture drift does not silently reopen. |
| 052 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-OPERATOR-EXECUTABLE-DISJOINT | Enforce the post-T1.2 invariant that `vre.operatorSurface.commands` and `vre.operatorSurface.doctorCommands` never intersect `vre.executableCommands`, so a command that has become really dispatched by `bin/vre` is no longer also claimed as a future/operator-only Phase 9 stub | `environment/control/capability-handshake.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json` | none | `node --test environment/tests/control/capability-handshake.test.js` (7/7 pass, now also pinning the disjoint invariant); `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass); `npm run test:phase9` (127 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); live smoke `node bin/vre capabilities --json` against the real repo -> exit 0, stderr empty, stdout byte-identical to `.vibe-science-environment/control/capability-handshake.json`; runtime diff against the live aggregator now emits `operatorSurface.commands.length == 10` (was 11) and zero intersection with `executableCommands` in both full-mode and degraded-no-kernel scenarios | verified | Round 36 adversarial closure on top of Round 35. After T1.2 landed, `capabilities --json` was correctly promoted into `DISPATCH_TABLE` and `IMPLEMENTED_PHASE9_COMMANDS` and therefore into `vre.executableCommands`, but it was left in `PHASE9_STUB_DEFINITIONS` as a Wave 0 stub entry and the aggregator still derived `vre.operatorSurface.commands` from that stub list without excluding already-executable commands. The live runtime therefore reported `capabilities --json` in BOTH `executableCommands` AND `operatorSurface.commands`, violating the Round 32 semantic that `operatorSurface` enumerates only future/operator-only commands. Closure: aggregator now filters `PHASE9_STUB_DEFINITIONS` against the live executable set for both `commands` and `doctorCommands`, a new control test pins the disjoint invariant, `valid-full.json` and `valid-degraded-no-kernel.json` drop `capabilities --json` from their `operatorSurface.commands` arrays, and `valid-degraded-no-kernel.json` additionally gets its `degradedReasons` sorted alphabetically (to match the aggregator's `uniqueSorted` invariant) and its `connectors`/`automations` arrays expanded to the full reviewed set (two connectors, three automations) so degradedReasons and the ontology arrays stay internally consistent. No runtime surface added, no frozen contract text changed. |
| 053 | 2026-04-22 | 1 | W1-PLUGIN-HANDSHAKE-INJECTOR | Land the plugin-side Phase 9 handshake injector helper that discovers sibling VRE, prefers live `capabilities --json`, accepts only fresh validated artifact fallback, and emits explicit degraded state instead of silently trusting stale awareness | `../vibe-science/plugin/scripts/handshake-inject.js`, `../vibe-science/tests/phase9-handshake-injection.test.mjs`, `../vibe-science/tests/kernel-bridge-projection-count.test.mjs` | `VIBE_PHASE9_HANDSHAKE_ONLY`, `VIBE_PHASE9_ENABLED` | `npm run test:phase9` in `vibe-science` (11/11 pass); `npm test` in `vibe-science` (171 E2E + 342 Phase 8 + 11 Phase 9 pass); live hook smoke with `VIBE_PHASE9_HANDSHAKE_ONLY=1` for `plugin/scripts/session-start.js` and `plugin/scripts/prompt-submit.js` | verified | Wave 1 `T1.3`. Helper mirrors the sibling-layout discovery philosophy used by `vre init`, validates the full required Phase 9 handshake field set before trusting artifact fallback, and degrades explicitly with `VRE_MISSING` or `HANDSHAKE_STALE_OR_UNAVAILABLE` rather than presenting stale capability state as current awareness. Same-pass truthfulness refinement: the digest now emits `nextAction: discovery, not research` only for transport-missing/transport-degraded states, not for truthful live payloads that merely carry degraded surfaces. |
| 054 | 2026-04-22 | 1 | W1-SESSIONSTART-HANDSHAKE-DIGEST | Inject a visible Phase 9 capability digest into plugin `SessionStart` when handshake-only or full Phase 9 mode is enabled | `../vibe-science/plugin/scripts/session-start.js`, `../vibe-science/plugin/scripts/handshake-inject.js`, `../vibe-science/tests/phase9-handshake-injection.test.mjs`, `../vibe-science/tests/kernel-bridge-projection-count.test.mjs` | `VIBE_PHASE9_HANDSHAKE_ONLY`, `VIBE_PHASE9_ENABLED` | `npm run test:phase9` in `vibe-science` (11/11 pass); `npm test` in `vibe-science` (171 E2E + 342 Phase 8 + 11 Phase 9 pass); live `node plugin/scripts/session-start.js` with `VIBE_PHASE9_HANDSHAKE_ONLY=1` -> digest visible, `vrePresent=true`, artifact path visible | verified | Wave 1 `T1.3`. Startup now shows executable commands, markdown-only contracts (explicitly marked docs-only), queueable task kinds, ontology counts, operator commands/artifact paths, active-objective state, memory freshness, and missing/degraded surfaces without dumping the full machine payload into the startup banner. Phase 9 disabled leaves startup unchanged. |
| 055 | 2026-04-22 | 1 | W1-PROMPTSUBMIT-HANDSHAKE-REINJECTION | Re-check or re-inject the Phase 9 handshake for research-relevant `UserPromptSubmit` prompts while keeping non-research prompts unbloated unless a fresh active objective is already visible | `../vibe-science/plugin/scripts/prompt-submit.js`, `../vibe-science/plugin/scripts/handshake-inject.js`, `../vibe-science/tests/phase9-handshake-injection.test.mjs`, `../vibe-science/tests/kernel-bridge-projection-count.test.mjs` | `VIBE_PHASE9_HANDSHAKE_ONLY`, `VIBE_PHASE9_ENABLED` | `npm run test:phase9` in `vibe-science` (11/11 pass); `npm test` in `vibe-science` (171 E2E + 342 Phase 8 + 11 Phase 9 pass); live `node plugin/scripts/prompt-submit.js` with `VIBE_PHASE9_HANDSHAKE_ONLY=1` -> research prompt shows digest, non-research prompt omits it while no objective is active | verified | Wave 1 `T1.3`. Research/resume/analysis/tool-discovery style prompts now refresh Phase 9 awareness instead of relying on startup-only knowledge. Non-research prompts stay compact unless a fresh artifact already shows an active objective, matching the frozen T1.3 no-bloat rule. |
| 056 | 2026-04-22 | 1 | W1-PROMPTSUBMIT-ACTIVE-OBJECTIVE-GATE-COVERAGE | Pin the positive T1.3 no-bloat exception with a dedicated regression test: non-research prompts stay compact only when no active objective is visible, but MUST still receive the handshake digest when a fresh artifact already shows an active objective | `../vibe-science/tests/phase9-handshake-injection.test.mjs`, `../vibe-science/tests/kernel-bridge-projection-count.test.mjs`, `../vibe-science/plugin/scripts/handshake-inject.js`, `../vibe-science/plugin/scripts/prompt-submit.js` | `VIBE_PHASE9_HANDSHAKE_ONLY`, `VIBE_PHASE9_ENABLED` | `npm run test:phase9` in `vibe-science` (12/12 pass); live non-research prompt smoke after temporarily setting `objective.activeObjectiveId=OBJ-123` in the fresh handshake artifact -> digest visible + `objective: OBJ-123 (active)`; `npm run check:phase9-ledger` explicit delta green | verified | Round 38 adversarial closure on top of seq `053-055`. The runtime branch for "non-research prompt, but a fresh active objective already exists" was working, but it was only verified by live smoke and not pinned by a positive regression test. This row hardens the frozen T1.3 exception so the plugin cannot silently regress to "never inject on non-research prompts" while still passing the original no-bloat test. No new runtime surface added; this is coverage hardening for an already-landed T1.3 contract branch. |
| 057 | 2026-04-22 | 1 | W1-CAPABILITY-HANDSHAKE-DRIFT-TESTS | Land the frozen T1.4 fail-closed drift suite for capability-handshake truth across docs-vs-dispatch, projection fixtures, queueable task kinds, reviewed ontology inventories, and memory API exposure | `environment/control/capability-handshake.js`, `environment/tests/control/capability-handshake-drift.test.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/ci/validate-counts.js`, `package.json` | none | `node --test environment/tests/control/capability-handshake-drift.test.js` (9/9 pass); `node --test environment/tests/control/capability-handshake.test.js environment/tests/schemas/phase9-capability-handshake.schema.test.js` (20/20 pass); `npm run test:phase9` (136 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators) | verified | Wave 1 `T1.4`. `capability-handshake.js` now classifies command surfaces through a shared helper so docs-only commands remain under `markdownOnlyContracts` while dispatch-without-doc commands stay executable but emit a reviewed warning in `degradedReasons`. The new drift suite fails closed when a projection fixture path disappears, when `queueableTaskKinds` diverge from the task registry, when any on-disk schema/connector/automation/domain-pack reviewed surface is omitted from the handshake ontology, or when reviewed memory API exposure drifts in either direction (allowlisted export missing on disk OR on-disk export missing from the reviewed allowlist). Canonical full + degraded fixtures were synced to the new executable-warning invariant. |
| 058 | 2026-04-22 | 2 | W2-OBJECTIVE-STORE | Create the durable Phase 9 objective store root and first canonical artifacts without introducing active-pointer lock semantics yet | `environment/objectives/store.js`, `environment/tests/control/objective-store.test.js`, `environment/tests/fixtures/phase9/objective/invalid-missing-stop-conditions.json`, `environment/tests/fixtures/phase9/objective/invalid-runtime-mode.json`, `environment/tests/schemas/phase9-objective.schema.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/control/objective-store.test.js` (3/3 pass); `node --test environment/tests/schemas/phase9-objective.schema.test.js` (6/6 pass); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass); `npm run build:surface-index` (30 total surfaces, including `objective-store`); `npm run test:phase9` (141 pass, 0 fail, 1 skipped); `npm run validate` (green after spec-side ledger + logs are updated); explicit `npm run check:phase9-ledger -- --changed-file=...` (green) | verified | Wave 2 `T2.1`. `createObjectiveStore()` validates `phase9.objective.v1` against the reviewed schema, creates the canonical `.vibe-science-environment/objectives/<objectiveId>/` tree, atomically writes `objective.json`, creates empty `events.jsonl` + `handoffs.jsonl`, and creates `digests/`. Invalid `stopConditions` and widened `runtimeMode` fail closed before any objective directory is created. Active pointer / lock / lifecycle transitions remain out of scope for `T2.1` and are reserved for `T2.2+`. |
| 059 | 2026-04-23 | 2 | W2-ACTIVE-OBJECTIVE-POINTER-LOCK | Land the active-objective pointer as the durable Wave 2 workspace lock, including atomic acquisition, `OBJECTIVE_LOCK_HELD`, null-initialized wake lease state, and pointer release only on terminal stop/completed paths | `environment/objectives/store.js`, `environment/tests/control/objective-lock.test.js`, `environment/tests/fixtures/phase9/active-objective-pointer/valid-active.json`, `environment/tests/schemas/phase9-active-objective-pointer.schema.test.js` | none | `node --test environment/tests/control/objective-lock.test.js` (5/5 pass); `node --test environment/tests/schemas/phase9-active-objective-pointer.schema.test.js` (2/2 pass); live CLI smoke in a fixture-root repo (`objective start` -> `OBJECTIVE_LOCK_HELD` on second start -> `objective pause` retains pointer -> `objective stop` releases pointer and clears handshake objective); `npm run test:phase9` (146 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators) | verified | Wave 2 `T2.2` lock slice. `activateObjective()` now acquires the canonical `.vibe-science-environment/objectives/active-objective.json` pointer atomically, records `lockAcquiredAt` / `lockAcquiredBySession`, and initializes `currentWakeLease` with all-null fields. A second concurrent start fails closed with the named error `OBJECTIVE_LOCK_HELD`, `objective pause` keeps the same pointer, and `objective stop` releases it while preserving the terminal objective record. No resume-snapshot writer, stale-lock reclaim path, or plugin startup loader landed in this row; those remain owned by `T2.4+`. |
| 060 | 2026-04-23 | 2 | W2-OBJECTIVE-LIFECYCLE-CLI-START-PAUSE-STOP | Upgrade the Wave 0 objective lifecycle stubs into the narrow real T2.2 CLI slice: `objective start`, `objective pause`, and `objective stop` now mutate the durable objective state and refresh the capability-handshake artifact immediately after each successful transition | `bin/vre`, `environment/objectives/store.js`, `environment/tests/control/objective-lock.test.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/cli/bin-vre-phase9-stubs.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json` | none | `node --test environment/tests/cli/bin-vre-phase9-stubs.test.js` (7/7 pass); `node --test environment/tests/control/capability-handshake.test.js` (7/7 pass); `node --test environment/tests/control/objective-lock.test.js` (5/5 pass); `node --test environment/tests/cli/*.test.js` (25/25 pass); live CLI smoke in a fixture-root repo (`objective start` writes handshake with active objective id, `objective pause` keeps the pointer and updates handshake status, `objective stop` clears the handshake objective); `npm run test:phase9` (146 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators) | verified | Wave 2 `T2.2` CLI slice. `objective start` now parses the reviewed budget / wake-policy inputs, creates the objective store, acquires the active pointer, and regenerates `.vibe-science-environment/control/capability-handshake.json` before post-objective artifacts can rely on it. `objective pause` and `objective stop` are also real and handshake-aware. `objective status`, `objective resume`, and `objective doctor` intentionally remain out of scope here and stay owned by `T2.3`; `objective pause` still does NOT write a resume snapshot yet because the canonical snapshot writer belongs to `T2.4`. |
| 061 | 2026-04-23 | 2 | W2-OBJECTIVE-LIFECYCLE-COVERAGE-HARDENING | Close the Round 46 test-coverage gaps left by seq 059-060: the implemented lifecycle error branches (stop without active pointer, stop with mismatched objectiveId, pause on already-paused objective, double stop after release) are now pinned by dedicated CLI regression tests | `environment/tests/control/objective-lock.test.js` | none | `node --test environment/tests/control/objective-lock.test.js` (9/9 pass; +4 new regression tests over the seq 059-060 baseline of 5/5); `npm run test:phase9` (150 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators) | verified | Round 46 adversarial closure on top of seq 059-060. The original T2.2 suite covered all 5 plan-mandated test bullets (double start, stop releases, pause retains, pointer atomic rollback, post-start handshake). Adversarial review found that four additional implemented error branches were present in `store.js` and `bin/vre` but not pinned by any dedicated test: (1) `stopObjective` failing with "No active objective pointer exists" when called without a pointer, (2) `stopObjective` rejecting a mismatched `--objective` argument while preserving the lock, (3) `pauseObjective` rejecting a second pause on an already-paused objective via `PAUSABLE_STATUSES`, and (4) a second `objective stop` after successful release correctly reporting the pointer is gone while leaving the terminal objective record untouched. Each new test also cross-checks that pointer state, handshake artifact, and objective record stay coherent after the failure path. Known follow-up (NOT closed in this round): non-lock-held lifecycle errors currently emit plain-text stderr instead of a structured JSON payload like `OBJECTIVE_LOCK_HELD`; this UX consistency gap is tracked for a future named-error pass, most naturally alongside `T2.3` (`objective status/resume/doctor`) which will widen the named-error surface. |
| 062 | 2026-04-23 | 2 | W2-OBJECTIVE-CLI | Land the T2.3 objective lifecycle CLI surface: structured JSON for lifecycle success/errors, explicit wake-policy validation, `objective status --json`, `objective resume`, blocker resolution, and snapshot repair without mutating immutable objective fields | `bin/vre`, `environment/objectives/cli.js`, `environment/tests/cli/objective-cli.test.js`, `environment/tests/control/objective-lock.test.js`, `environment/tests/cli/bin-vre-phase9-stubs.test.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/cli/objective-cli.test.js` (7/7 pass); `node --test environment/tests/control/objective-lock.test.js` (9/9 pass); `node --test environment/tests/control/capability-handshake.test.js` (7/7 pass); `node --test environment/tests/schemas/phase9-capability-handshake.schema.test.js` (13/13 pass); adversarial live attack matrix A1-A5 (wake-policy fail-closed, interactive manual default, pause/status summary, divergent snapshot repair, `BLOCKER.flag` blocker-resolve ordering) all defended; `npm run test:phase9` (157 pass, 0 fail, 1 skipped) | verified | Wave 2 `T2.3`. `objective start` now fails closed with structured `E_WAKE_POLICY_REQUIRED` for unattended modes without explicit wake policy while interactive mode uses an explicit manual/no-op wake policy. `objective status --json` reports the canonical objective summary (status, runtime/reasoning mode, wake lease health, capability summary, iteration/wall-clock budget, blocker/stop signals, and artifact pointers). `objective pause` and `objective stop` now write resume snapshots; `objective resume` reactivates paused/blocked objectives, writes `blocker-resolve` before removing `BLOCKER.flag`, supports `--repair-snapshot`, appends `state-repair(repairedLayer=snapshot)`, and preserves immutable `objective.reasoningMode`. Lifecycle error branches now return structured JSON payloads through `ObjectiveCliError`, closing the UX consistency follow-up noted in seq 061 for the T2.3-owned commands. `objective doctor` remains a Wave 0 stub by design; no stale-lock reclaim, reusable reconciliation engine, or plugin startup loader is introduced here. Same-pass bookkeeping cleanup: the seq 061 notes now correctly say Round 46 instead of the stale Round 44 label. |
| 063 | 2026-04-23 | 2 | W2-OBJECTIVE-RESUME-STRUCTURED-ERROR-FIX | Fix the missing `await` before `withLock` in `resumeObjectiveCommand` so that resume error branches actually reach `coerceObjectiveCliError` and become structured JSON through `ObjectiveCliError`, closing the contradiction between the seq 062 claim and the real post-T2.3 runtime | `environment/objectives/cli.js`, `environment/tests/cli/objective-cli.test.js` | none | `node --test environment/tests/cli/objective-cli.test.js` (9/9 pass; +2 new regression tests pinning `resume-when-active` -> `E_OBJECTIVE_STATE_INVALID` and `resume-after-stop` -> `E_ACTIVE_OBJECTIVE_POINTER_MISSING`); `npm run test:phase9` (159 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); live repro pre-fix: `objective resume` of an active objective emitted plain-text stderr `vre: objective resume: Cannot resume objective in status active`; live repro post-fix: same scenario emits `{ok:false, code:"E_OBJECTIVE_STATE_INVALID", transition:"resume", status:"active"}` via `writeJsonPayload` | verified | Round 48 adversarial closure on top of seq 062. Seq 062 claimed that "Lifecycle error branches now return structured JSON payloads through ObjectiveCliError, closing the UX consistency follow-up noted in seq 061 for the T2.3-owned commands". Adversarial re-review confirmed that the claim held for `objective start`, `objective pause`, `objective stop`, and `objective status`, but NOT for `objective resume`: two resume error branches fell through the dispatcher `ObjectiveCliError` catcher (`bin/vre:1117`) and emitted plain-text stderr via the generic handler (`bin/vre:1128`). Root cause: `resumeObjectiveCommand` used `return withLock(...)` instead of `return await withLock(...)` at `environment/objectives/cli.js:659`, so the outer `try/catch` exited synchronously before the inner `withLock` promise rejected, and raw `Error` objects (not `ObjectiveCliError`) bubbled up to `runObjectiveResume`. Fix: one-line `return` -> `return await` change, plus a code comment explaining the semantics so a future refactor cannot regress it silently. Known follow-up, NOT closed here: the canonical resume-snapshot writer currently lives in `cli.js` (full `phase9.resume-snapshot.v1` field set) rather than in `environment/objectives/resume-snapshot.js` as the `T2.4` plan prescribes; this is a soft scope-creep that `T2.4` will refactor when it lands, without introducing a new writer. Also deliberately NOT closed here: seq 061 was edited in-place to say Round 46 instead of Round 44; this deviates from the strict append-only rule, is transparently disclosed in seq 062 and in this row, and is accepted as a one-time bookkeeping exception, not as a pattern. |
| 064 | 2026-04-23 | 2 | W2-RESUME-SNAPSHOT-WRITER | Refactor the canonical Phase 9 resume snapshot writer out of the objective CLI into a reviewed `environment/objectives/resume-snapshot.js` API that Wave 4 can call after loop iterations, heartbeat no-ops, blocker transitions, handoffs, pause/stop, pre-compact, and manual snapshot commands | `environment/objectives/resume-snapshot.js`, `environment/objectives/cli.js`, `environment/objectives/store.js`, `environment/tests/control/resume-snapshot.test.js`, `environment/tests/cli/objective-cli.test.js`, `environment/tests/fixtures/phase9/resume-snapshot/valid-heartbeat.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-missing-queue-visibility.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-missing-next-action.json`, `environment/tests/fixtures/phase9/resume-snapshot/invalid-missing-open-handoffs.json`, `environment/tests/schemas/phase9-resume-snapshot.schema.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/control/resume-snapshot.test.js` (2/2 pass); `node --test environment/tests/cli/objective-cli.test.js` (10/10 pass; +1 stale kernel-fingerprint resume block regression); `node --test environment/tests/schemas/phase9-resume-snapshot.schema.test.js` (10/10 pass; +1 valid heartbeat fixture and +3 required-field negative fixtures); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass); `npm run build:surface-index` (31 total surfaces, including `resume-snapshot`); `npm run test:phase9` (166 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators) | verified | Wave 2 `T2.4`. This is a refactor-not-reimplementation of the writer that T2.3 temporarily hosted in `environment/objectives/cli.js`: `writeResumeSnapshot`, `writeObjectiveResumeSnapshot`, `readResumeSnapshot`, `detectSnapshotDivergence`, `appendObjectiveEvent`, and `readBlockerFlag` now live in `environment/objectives/resume-snapshot.js` and validate through the reviewed schema host fallback. The new API writes the full `phase9.resume-snapshot.v1` required field set, mirrors `activePointer.currentWakeLease` without making the snapshot the lease authority, preserves immutable objective fields such as `reasoningMode`, and gives Wave 4 a direct callable surface for heartbeat / loop-iteration / handoff / pre-compact / manual snapshot triggers. `objective pause`, `objective stop`, `objective resume --repair-snapshot`, and `objective status --json` now consume the shared module instead of carrying a private writer. Required T2.4 fixture coverage is expanded with `valid-heartbeat`, missing `queueVisibility`, missing `nextAction`, and missing `openHandoffs`; stale/invalid kernel fingerprint now blocks `objective resume` through structured `E_RESUME_SNAPSHOT_INVALID` while leaving the paused objective state untouched. No plugin startup loader, unattended runtime loop, stale-lock reclaim, or separate active pointer source is introduced here; those remain owned by later Wave 2 / Wave 4 tasks. |
| 065 | 2026-04-23 | 2 | W2-RESUME-SNAPSHOT-WRITER-WAVE4-TRIGGER-COVERAGE | Pin the Wave 4 trigger claim made in seq 064 (`gives Wave 4 a direct callable surface for heartbeat / loop-iteration / handoff / pre-compact / manual snapshot triggers`) with dedicated writer-level regression tests and a valid `pre-handoff` fixture, closing the coverage gap where `pre-handoff` and `loop-iteration` trigger reasons were enumerated in the schema but not exercised through `writeObjectiveResumeSnapshot` | `environment/objectives/resume-snapshot.js`, `environment/tests/control/resume-snapshot.test.js`, `environment/tests/schemas/phase9-resume-snapshot.schema.test.js`, `environment/tests/fixtures/phase9/resume-snapshot/valid-pre-handoff.json` | none | `node --test environment/tests/control/resume-snapshot.test.js` (5/5 pass; +3 new regression tests pinning `writtenReason: pre-handoff` end-to-end, `writtenReason: loop-iteration` end-to-end, and `writeObjectiveResumeSnapshot` fail-closed on mismatched injected `objectiveRecord`); `node --test environment/tests/schemas/phase9-resume-snapshot.schema.test.js` (11/11 pass; +1 `valid-pre-handoff.json` fixture); `npm run test:phase9` (170 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on full T2.4 + Round 50 delta) | verified | Round 50 adversarial closure on top of seq 064. Seq 064 claim language (`heartbeat / loop-iteration / handoff / pre-compact / manual snapshot triggers`) had only two of the five triggers exercised at the writer level (`heartbeat` via `resume-snapshot.test.js` test 1 and `pre-compact` via test 2). `operator-pause`, `pre-stop`, and `manual` were pinned transitively through the CLI tests (`objective-cli.test.js` pause/stop/resume --repair-snapshot). `loop-iteration` existed only as a schema fixture (`valid-mid-loop.json`), with no writer-level invocation, and `pre-handoff` had neither fixture nor test. This is the exact same class of gap Round 48 closed for `objective resume`: a ledger claim made transparently but not actually pinned by a test. Round 50 adds (a) `valid-pre-handoff.json` with an enum-valid `nextAction.kind=enqueue-task` carrying an explicit `blockedByHandoffId` param, registered in the schema test; (b) a writer-level `pre-handoff` trigger test that asserts schema-valid output AND a clean round-trip through `readResumeSnapshot`; (c) a writer-level `loop-iteration` trigger test that also asserts the event counter decrements `budgetRemaining.maxIterationsLeft` (20 -> 19) after `appendObjectiveEvent(kind=loop-iteration)`; and (d) a writer-level regression test for the `writeObjectiveResumeSnapshot` defensive guard at `resume-snapshot.js:334-338` that throws when a caller injects an `objectiveRecord` whose `objectiveId` does not match the `objectiveId` argument, with a post-rejection assertion that `resume-snapshot.json` was never written. No runtime surface added; this is pure coverage hardening. |
| 066 | 2026-04-23 | 2 | W2-PLUGIN-STARTUP-OBJECTIVE-LOADER | Land the plugin-side startup objective loader so SessionStart reads the active pointer, durable objective record, and resume snapshot, then injects a visible objective digest or an explicit blocker instead of silent omission | `../vibe-science/plugin/scripts/objective-loader.js`, `../vibe-science/plugin/scripts/session-start.js`, `../vibe-science/tests/phase9-handshake-injection.test.mjs` | `VIBE_PHASE9_HANDSHAKE_ONLY`, `VIBE_PHASE9_ENABLED` | `node --test tests/phase9-handshake-injection.test.mjs` (15/15 pass, including no-objective, active-objective, broken-pointer, stale-snapshot, real SessionStart injection, and Phase 9 disabled); `npm run test:phase9` in `vibe-science` (17/17 pass); `npm test` in `vibe-science` (171 E2E + 342 Phase 8 + 17 Phase 9 pass) | verified | Wave 2 `T2.5`. The new plugin module is startup-only and deliberately does NOT widen `prompt-submit`: it reads `.vibe-science-environment/objectives/active-objective.json`, resolves the immutable objective record, reads `resume-snapshot.json`, and injects `[PHASE9 OBJECTIVE DIGEST]` into SessionStart context. When durable state is broken it fails closed with an explicit blocker summary (`E_ACTIVE_POINTER_ORPHANED`, `E_RESUME_SNAPSHOT_MISSING`, `E_RESUME_SNAPSHOT_DIVERGED`, `E_REASONING_MODE_DIVERGED`, or `E_STATE_CONFLICT`) rather than suppressing the objective state. The real-session integration test writes temporary sibling-VRE objective artifacts and proves SessionStart sees a live active objective plus resume state; the old synthetic active-pointer test was also corrected to use the canonical pointer path instead of a snapshot path. This row is intentionally sibling-only and therefore inventory-ineligible: no new VRE machine-discoverable surface was introduced and `phase9-vre-surface-index.json` remains unchanged. |
| 067 | 2026-04-23 | 2 | W2-STATUS-LEDGER-METRICS-REFRESH | Close the Round 51 half-update drift where `16-implementation-status-ledger.md` row 85 added `seq 066` and `objective-loader.js` for T2.5 but kept VRE-side metrics frozen at the pre-T2.x baseline (`127 pass` test:phase9, `29 total surfaces`, `25/25 cli`, `43/43 schemas`, `7/7 + 59/59 control/lib`), even though T2.3 / T2.4 / Round 50 / T2.5 had each moved those numbers forward | `../vibe-science/blueprints/private/phase9-vre-autonomous-research-loop/16-implementation-status-ledger.md` | none | `npm run test:phase9` in VRE (170 pass, 0 fail, 1 skipped); `node --test environment/tests/cli/*.test.js` (35/35 pass); `node --test environment/tests/control/*.test.js` (92/92 pass, including 7/7 capability-handshake); `node --test environment/tests/lib/*.test.js` (149/149 pass); `node --test environment/tests/schemas/phase9-*.test.js` (50/50 pass); `phase9-vre-surface-index.json.length === 32`; `npm run validate` (green, 12/12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 52 delta including spec-side paths) | verified | Round 52 adversarial closure on top of seq 066. Seq 066 (T2.5 plugin startup objective loader) was itself sound: loader + session-start wiring + 4 frozen test scenarios + integration smoke all landed correctly and CI remained green. Adversarial re-review, applying `superpowers:systematic-debugging` Phase 1 evidence gathering, found that the Round 51 edit to spec-side `16-implementation-status-ledger.md` row 85 (the meta-cluster `15-phase8-dependency-map.md` Phase 8 required surfaces cluster) added `seq 066`, `objective-loader.js`, and the vibe-science-side `17/17 pass` + `171 E2E + 342 Phase 8 + 17 Phase 9` numbers, but left VRE-side test-count and surface-index metrics frozen at their T1.4 values. This violated the spirit of the `16-implementation-status-ledger.md` Update Rule that says "do not mark a row `verified` without naming concrete test or evidence" — the row was still `in-progress`, but it was edited for Round 51 and therefore should have carried refreshed VRE numbers, not ones that now misrepresented the current state by ~330n the test:phase9 count. Round 52 refreshes only the factually stale numbers on that single row (test:phase9 `127 -> 170`, surface-index `29 -> 32`, cli tests `25 -> 35`, control tests `7+59 -> 92 including 7 capability-handshake`, schema tests `43 -> 50`, and adds the previously-unlisted `lib tests 149/149 pass`), tags each refresh with `post-Round-52`, and preserves the pre-T2.x baseline as a parenthetical reference so the rolling delta stays auditable. No runtime surface added; no new test; pure ledger hygiene. Out of scope for this round and transparently NOT closed here: (a) the pre-existing warning in vibe-science CI about `archive/vibe-science-v5.5-codex` being a gitlink (mode 160000) without a declaring `.gitmodules` file, first introduced by vibe-science commit `68b1690`, does not block CI and is not a Round 51 regression — it will be addressed in a dedicated cleanup pass that is safe to execute outside the Phase 9 Wave 2 freeze corridor; (b) the broader row 83 (Resume snapshot + reconciliation) remains correctly `in-progress` because reconciliation engine and Wave 4 runtime consumers are not yet landed. |
| 068 | 2026-04-23 | 3 | W3-ANALYSIS-MANIFEST-SCHEMA | Land the Phase 9 analysis-manifest schema, canonical fixtures, and a narrow pre-execution validator so manifest validation can run without executing the script before `run-analysis` or the positive plugin gate land | `environment/schemas/phase9-analysis-manifest.schema.json`, `environment/orchestrator/analysis-manifest.js`, `environment/tests/fixtures/phase9/analysis-manifest/valid-python.json`, `environment/tests/fixtures/phase9/analysis-manifest/valid-notebook.json`, `environment/tests/fixtures/phase9/analysis-manifest/invalid-missing-objective-id.json`, `environment/tests/fixtures/phase9/analysis-manifest/invalid-destructive-without-approval.json`, `environment/tests/fixtures/phase9/analysis-manifest/invalid-nonexistent-experiment-manifest.json`, `environment/tests/schemas/phase9-analysis-manifest.schema.test.js`, `environment/tests/control/analysis-manifest.test.js`, `environment/control/capability-handshake.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/schemas/phase9-analysis-manifest.schema.test.js` (4/4 pass); `node --test environment/tests/control/analysis-manifest.test.js` (4/4 pass); `node --test environment/tests/control/capability-handshake.test.js` (7/7 pass); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass); `npm run build:surface-index` (33 total surfaces, including 10 schema surfaces and new `phase9.analysis-manifest.v1`); `npm run test:phase9` (178 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on full Round 53 delta including spec-side paths) | verified | Wave 3 `T3.1`. The new schema pins the required execution contract fields for `objectiveId`, `experimentId`, `analysisId`, script/notebook reference, input/output objects, command runner/argv, budget, safety flags, canonical task kind `analysis-execution-run`, expected artifacts, human approval metadata, and author timestamps. The narrow validator is intentionally pre-execution only: it validates the schema, requires an active objective pointer, checks that the immutable objective record exists, accepts either an existing `EXP-###` manifest binding or a same-transaction experiment-registration stub, and fails closed when destructive/tree-wide execution lacks explicit approval. This closes the frozen T3.1 test bullets without prematurely landing `run-analysis`, execution-lane writes, or the plugin-side positive VRE command gate. Same-patch truthfulness updates were required: the capability handshake no longer reports `analysis-manifest schema` as missing, the fixture guesser now recognizes `valid-python.json` / `valid-notebook.json`, and the machine surface inventory/runtime-contract validators now track the tenth Phase 9 schema surface. |
| 069 | 2026-04-23 | 3 | W3-ANALYSIS-MANIFEST-ORCHESTRATOR-SURFACE | Close the Round 53 half-update drift where `seq 068` landed `environment/orchestrator/analysis-manifest.js` as a real orchestrator module and cited it in the row paths, but did not register it as an `orchestrator-surface` entry in `phase9-surface-index.js` (the machine-generated VRE inventory), breaking the pattern established by `environment/objectives/store.js` / `cli.js` / `resume-snapshot.js` and leaving the path coverage-eligible but inventory-invisible | `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass, +1 `analysis-manifest` orchestrator-surface assertion and fixture-repo count 21 -> 22); `npm run build:surface-index` (34 total surfaces, +1 over seq 068 baseline; both `analysis-manifest` orchestrator-surface and `phase9.analysis-manifest.v1` schema now machine-tracked); `npm run test:phase9` (178 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 54 delta including spec-side paths) | verified | Round 54 adversarial closure on top of seq 068. Seq 068 (T3.1 analysis-manifest schema + reusable validator) landed correctly: 10 schemas reviewed, 4 fixtures pinned, schema + control tests 4/4 each, capability-handshake no longer reporting the schema as `missingSurface`, handshake fixtures extended to 10 schema entries. Adversarial re-review, applying `superpowers:systematic-debugging` Phase 1 evidence gathering against the isInventoryTrackablePath / isCoveredVrePath semantics introduced in Round 22 (`seq 011`) and hardened in Round 30 (`seq 042 E_LEDGER_PHANTOM_PATH`), found that `environment/orchestrator/analysis-manifest.js` was listed in `seq 068` row paths (and `environment/orchestrator/` is a covered path per check-phase9-ledger.js:30), but the file was NOT registered in `phase9-surface-index.js` as an `orchestrator-surface` entry the way sibling modules (`store.js`, `cli.js`, `resume-snapshot.js`, `capability-handshake.js`, `time-provider.js`) all are. The orphan check passed for `seq 068` only because another path in the same row (the `.schema.json`) matched the `phase9.analysis-manifest.v1` schema surface — exactly the silent-skip class that Round 22 closed by tightening isInventoryTrackablePath, and Round 30 hardened by adding E_LEDGER_PHANTOM_PATH for mixed-path rows whose non-matching paths do not exist on disk. Round 54 closes the residual case where the mixed-path masking applies even when the non-matching path DOES exist on disk: by registering `analysis-manifest` as its own `orchestrator-surface` in the inventory builder, seq 068 can no longer carry a legitimate file on a covered path that is inventory-invisible. Same-pass bookkeeping: `16-implementation-status-ledger.md` row 85 (Phase 8 required surfaces) was edited in the same patch to refresh the stale VRE metrics that had accumulated through T3.1 (`test:phase9` `170 -> 178`, control tests `92 -> 96 including 4 analysis-manifest`, schema tests `50 -> 54 including 4 analysis-manifest`, surface-index `32 -> 34 including analysis-manifest orchestrator-surface`, plus seq citations `066 -> 066, 068, 069` and the analysis-manifest schema + orchestrator paths added to the row paths). No new runtime surface added beyond the inventory registration; this is pure truthfulness hardening of the machine inventory and the spec-side status overlay. Out of scope, deliberately NOT closed: (a) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning on vibe-science CI (predates Phase 9 Wave 3 by many commits, does not block CI, remains a dedicated cleanup candidate outside the freeze corridor); (b) row 83 (Resume snapshot + reconciliation) stays `in-progress` because Wave 4 reconciliation engine and runtime consumers remain non-landed. |
| 070 | 2026-04-23 | 3 | W3-EXPERIMENT-MANIFEST-BINDING | Bind existing VRE experiment manifests into `phase9.objective.v1.artifactsIndex.experiments[]` through a reviewed adapter/writer path without inventing a parallel `phase9.experiment-manifest.v1` store | `environment/objectives/store.js`, `environment/orchestrator/experiment-binding.js`, `environment/orchestrator/analysis-manifest.js`, `environment/tests/control/analysis-manifest.test.js`, `environment/tests/control/experiment-binding.test.js`, `environment/tests/fixtures/phase9/experiment-binding/legacy-vre-experiment-manifest.json`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/control/experiment-binding.test.js` (3/3 pass); `node --test environment/tests/control/analysis-manifest.test.js` (4/4 pass, safe path now creates the objective/experiment binding through the real helper instead of relying on a pre-seeded fixture array); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass); `npm run build:surface-index` (35 total surfaces, including new `experiment-binding` orchestrator-surface); `npm run test:phase9` (181 pass, 0 fail, 1 skipped) | verified | Wave 3 `T3.2`. `writeObjectiveArtifactsIndex()` is now the canonical objective-side writer for `artifactsIndex` mutations, and the new `environment/orchestrator/experiment-binding.js` adapter reads the existing `vibe.experiment.manifest.v1` store through `readManifest()`, rejects invalid `EXP-###` ids via that existing manifest contract, and appends bindings into `objective.artifactsIndex.experiments[]` with a durable `lastUpdatedAt` refresh. `analysis-manifest.js` now resolves existing experiment references through the reviewed binding adapter instead of duplicating manifest-read logic, while the happy-path control test now binds an existing experiment through the real helper before validating the analysis manifest. A legacy experiment-manifest fixture proves that old VRE manifests remain visible to Phase 9 without inventing a new primary schema or store. Same-patch truthfulness fallout updates the machine inventory/runtime-contract pinning so `experiment-binding` is a first-class orchestrator-surface and `test:phase9` explicitly includes the new control test. No `run-analysis` executor, execution-lane write, or plugin-side positive gate lands here; `T3.3` and `T3.4` remain pending. |
| 071 | 2026-04-23 | 3 | W3-EXPERIMENT-MANIFEST-BINDING-LOCK-FIX | Close the seq-070 read-modify-write race in `bindExperimentManifestToObjective`: two concurrent bind calls on the same objective would both read the same `artifactsIndex.experiments` baseline and silently overwrite each other on `writeObjectiveRecord`, because `atomicWriteUtf8` guarantees only file-level atomicity while the read-compute-write sequence lived outside any lock | `environment/objectives/store.js`, `environment/orchestrator/experiment-binding.js`, `environment/tests/control/experiment-binding.test.js` | none | `node --test environment/tests/control/experiment-binding.test.js` (4/4 pass post-Round-56, +1 concurrent-bind regression test that does `Promise.all([bindExperimentManifestToObjective(EXP-021), bindExperimentManifestToObjective(EXP-002)])` and asserts both bindings survive); `node --test environment/tests/control/analysis-manifest.test.js` (4/4 pass, downstream consumer unchanged); `npm run test:phase9` (182 pass post-Round-56, 0 fail, 1 skipped, +1 over the seq-070 181 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 56 delta including spec-side paths) | verified | Round 56 adversarial closure on top of seq 070. Seq 070 (T3.2 experiment-manifest binding) landed a reviewed adapter `environment/orchestrator/experiment-binding.js` and a new `writeObjectiveArtifactsIndex(projectPath, objectiveId, artifactsIndex, options)` exit in `environment/objectives/store.js`, and pinned 3 sequential tests that matched the frozen T3.2 test bullets. Adversarial re-review, applying `superpowers:systematic-debugging` Phase 1 evidence gathering, found that `bindExperimentManifestToObjective` did `readExistingExperimentManifest` -> `readObjectiveRecord` -> `listObjectiveExperimentBindings` -> `writeObjectiveArtifactsIndex(..., [...existing, newId])` all without any lock, so two concurrent bind calls could produce lost-update: both reads observe the same empty baseline, both writers compute their own complete array, and the second `atomicWriteUtf8` silently overwrites the first. This is not a T3.2 contract violation (the three frozen tests are sequential) but it is a real runtime vulnerability for later-wave consumers (Wave 4 runtime loop, plugin startup loader, etc.) that may reasonably fan out bindings in parallel. Round 56 closure: `writeObjectiveArtifactsIndex` is replaced by `mutateObjectiveArtifactsIndex(projectPath, objectiveId, mutator, options)` where the mutator runs inside `withLock(projectRoot, ${objectiveId}-objective.json, ...)` against the freshly read artifactsIndex; the caller (experiment-binding) now passes a functional transform (`(current) => current includes id ? current : {...current, experiments: [...dedup, id]}`), so the read, the merge, and the write all happen atomically under the same per-objective record lock. `withLock` is not reentrant, so the lock lives in the store and NOT at the caller; same lock-name shape as `appendObjectiveEvent` in `resume-snapshot.js`. The no-op path (experiment already bound) now short-circuits with an identity return so the store skips a redundant write. A new regression test `preserves both bindings when two concurrent bind calls race on the same objective` runs `Promise.all(...)` on two different EXP ids and asserts that both ids land in the persisted objective record; this test was confirmed to RED against the pre-Round-56 baseline (observed output `actual: [ EXP-002 ]`, expected both EXP-021 + EXP-002) and GREEN against the Round-56 code. No new runtime surface added; no new schema; no in-place edit of earlier VRE ledger seq rows. Out of scope, transparently NOT closed here: (a) `environment/objectives/store.js` exposes no other read-modify-write exit that would benefit from the same lock today; if later waves add more mutators of `objective.json`, they must reuse `mutateObjectiveArtifactsIndex` or acquire the same lock explicitly; (b) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning in vibe-science CI predates Phase 9 Wave 3 and is not a Round 56 concern. |
| 072 | 2026-04-23 | 3 | W3-PLUGIN-POSITIVE-VRE-COMMAND-GATE | Land the frozen T3.3 plugin-side positive VRE command gate: production Bash may permit only the discovered sibling `node <vre>/bin/vre run-analysis --manifest <literal-path>` shape when the Phase 9 flag is enabled and the manifest already exists + validates; all other direct interpreter or extensionless node-script paths remain denied under Opzione B | `../vibe-science/plugin/scripts/pre-tool-use.js`, `../vibe-science/tests/phase9-vre-command-gate.test.mjs`, `../vibe-science/package.json` | `VIBE_PHASE9_ENABLED`, `VIBE_PHASE9_HANDSHAKE_ONLY` | `node --test tests/phase9-vre-command-gate.test.mjs` (5/5 pass: direct `python script.py` denied; sibling `node <vre>/bin/vre run-analysis --manifest <literal-path>` allowed; `node other/bin/vre ...` denied; variable manifest path denied; missing feature flag denied); `node --test tests/governance-hooks.test.mjs` (162/162 pass, proving the new gate does not weaken guardrail substrate or the Opzione B nuclear baseline); `npm run test:phase9` in `vibe-science` (22/22 pass); `npm test` in `vibe-science` (green: 171 E2E + 342 Phase 8 + 22 Phase 9 pass); `npm run check:phase9-ledger -- --changed-file=...` explicit green on the T3.3 delta including spec-side paths | verified | Wave 3 `T3.3`. `plugin/scripts/pre-tool-use.js` now parses only a narrow `node <extensionless-script>` corridor and opens exactly one production exception: the discovered sibling VRE `bin/vre` entrypoint with first subcommand `run-analysis`, a single `--manifest` flag, and a literal manifest path that already exists inside the sibling VRE root. The hook reuses the sibling discovery / Phase 9 flag semantics from `handshake-inject.js`, dynamically loads the sibling VRE `analysis-manifest.js` validator, and denies if the manifest path is variable/computed, outside the VRE root, missing, or semantically invalid. Same-pass adversarial closure fixed a regression where the first draft intercepted `node payload.mjs` before the existing nuclear external-script branch; the final gate is deliberately limited to extensionless `node` script paths so direct `node *.mjs` still flows through the pre-existing Opzione B deny path. This row is intentionally sibling-only and inventory-ineligible: no VRE machine surface changed, no new schema landed, and `run-analysis` itself remains owned by `T3.4`. |
| 073 | 2026-04-23 | 3 | W3-RUN-ANALYSIS-EXECUTION-LANE | Land frozen `T3.4` real sanctioned execution: `run-analysis` validates a reviewed manifest, requires the active objective lock and bound experiment, executes only the approved safe script corridor, captures stdout/stderr + required artifacts, appends `phase9.lane-run-record.v1` running/final records, and writes objective `analysis-run` events while keeping generic script execution blocked | `bin/vre`, `environment/control/capability-handshake.js`, `environment/orchestrator/execution-lane.js`, `environment/orchestrator/ledgers.js`, `environment/tests/cli/run-analysis.test.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/fixtures/phase9/lane-run-record/valid-running.json`, `environment/tests/schemas/phase9-lane-run-record.schema.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-counts.js`, `environment/tests/ci/validate-runtime-contracts.js`, `package.json`, `phase9-vre-surface-index.json` | `VRE_RUN_ANALYSIS_TIMEOUT_MS` | `node --test environment/tests/cli/run-analysis.test.js` (5/5 pass, pinning dry-run preview, successful safe execution, failing script, missing required output, missing objective lock, and sorted `listPhase9LaneRuns()` regression on numeric `recordSeq`); `node --test environment/tests/schemas/phase9-lane-run-record.schema.test.js` (4/4 pass, including the new `valid-running.json` pre-completion fixture); `node --test environment/tests/control/capability-handshake.test.js environment/tests/control/capability-handshake-drift.test.js` (16/16 pass, including truthful `run-analysis` executable/missing-surface state in canonical fixtures); `npm run build:surface-index` (37 total surfaces, including `execution-lane` and `orchestrator-ledgers`); `npm run test:phase9` (188 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators) | verified | Wave 3 `T3.4`. `bin/vre` now dispatches real `run-analysis`, surfaces structured `RunAnalysisCliError` payloads, and includes the command in the executable Phase 9 surface. `environment/orchestrator/execution-lane.js` implements the sanctioned scripts-only runner: it reuses `readAndValidateAnalysisManifest`, requires an active objective pointer, confirms the experiment is already bound, rejects notebooks/alternate runners/network/external-call/destructive/tree-wide execution, verifies literal manifest/script/input/output paths, writes a pre-run `phase9.lane-run-record.v1` record with `status:"running"`, executes the safe Node script with sanitized env and bounded timeout, persists stdout/stderr logs under `.vibe-science-environment/orchestrator/analysis-run-logs/`, verifies required outputs, appends the final lane-run record, and appends an objective `analysis-run` event. `environment/orchestrator/ledgers.js` now stores both legacy `vibe-orch.lane-run-record.v1` rows and Phase 9 lane-run rows in the same append-only file while filtering/validating each view by schemaVersion. Same-pass adversarial closure fixed two real drifts before landing: (1) `listPhase9LaneRuns()` originally sorted numeric `recordSeq` through `localeCompare`, which crashed on real Phase 9 reads; sorting is now numeric for numbers and lexical for strings, and the regression is pinned in `run-analysis.test.js`; (2) canonical capability-handshake fixtures were stale after `run-analysis` became real, still reporting it as missing/operator-only. The fixtures now truthfully show `run-analysis` as executable, remove it from `missingSurfaces`, remove stale `plugin handshake injection`, and retain the reviewed degraded warning that the executable lacks a markdown contract. Notebooks remain explicitly deferred, generic script execution remains blocked, and the plugin-side positive Bash gate from `seq 072` stays the only sanctioned production entry corridor. |
| 074 | 2026-04-23 | 3 | W3-RUN-ANALYSIS-TIMEOUT-ENV-CAP | Close the seq-073 ledger half-claim where `VRE_RUN_ANALYSIS_TIMEOUT_MS` was documented as a feature flag but the runtime never honoured it; operators could not cap a permissive `budget.maxRuntimeSeconds` manifest without editing the manifest itself | `environment/orchestrator/execution-lane.js`, `environment/tests/cli/run-analysis.test.js` | `VRE_RUN_ANALYSIS_TIMEOUT_MS` | `node --test environment/tests/cli/run-analysis.test.js` (6/6 pass post-Round-59, +1 regression test `VRE_RUN_ANALYSIS_TIMEOUT_MS=400` on a manifest budget of `60s` with a 5-second sleep script that asserts `status: interrupted` and `laneRuns.at(-1).exitCode === null`); `npm run test:phase9` (189 pass, 0 fail, 1 skipped, +1 over the T3.4 188 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 59 delta including spec-side paths) | verified | Round 59 adversarial closure on top of seq 073. Seq 073 ledger row explicitly cited `VRE_RUN_ANALYSIS_TIMEOUT_MS` in its feature-flags column, but independent adversarial review found ZERO references to that env var in the runtime code; the only occurrence in the repository was the ledger claim itself. `executeApprovedManifest` derived `timeoutMs` solely from `manifest.budget.maxRuntimeSeconds`, which is `integer, minimum: 1` in the schema with no upper bound, so a manifest could legally request a multi-hour runtime with no operator-level override. Round 59 closure: a new `resolveRunAnalysisTimeoutMs(maxRuntimeSeconds, env = process.env)` helper reads `VRE_RUN_ANALYSIS_TIMEOUT_MS` from the environment, parses it strictly as a positive integer, and returns `Math.min(parsed, manifestTimeoutMs)` so the env var can only REDUCE the manifest budget and NEVER extend it; invalid / empty / non-numeric values fall back to the manifest budget unchanged. A new end-to-end regression test sets `VRE_RUN_ANALYSIS_TIMEOUT_MS=400` against a 5-second sleep script with `maxRuntimeSeconds: 60`, asserts the process is killed within the cap, the final lane-run record is `status: interrupted` with `exitCode: null`, and was confirmed RED-first against the pre-Round-59 code (observed `actual: failed` because the uncapped 5s run finished before missing-output detection). No new runtime surface beyond the env var honouring; no new schema; no in-place edit of seq 073. Out of scope, deliberately NOT closed: (a) capability-handshake does not yet advertise `VRE_RUN_ANALYSIS_TIMEOUT_MS` in its env-var inventory (Phase 9 v1 handshake enumerates only feature flags, not operator caps); (b) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning in vibe-science CI predates Wave 3 and remains a dedicated cleanup candidate outside the freeze corridor. |
| 075 | 2026-04-23 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Land frozen `T4.1` one-slice autonomy runtime: `research-loop` now executes exactly one deterministic unattended slice under `rule-only`, requires the active objective lock, enforces duplicate/stale wake lease semantics, records queue/event write-ahead state, repairs a missing final resume snapshot from durable artifacts, blocks with `E_LLM_REASONING_REQUIRED` when no sanctioned next slice exists, and can pause on `SEMANTIC_DRIFT_DETECTED` before continuing | `bin/vre`, `environment/orchestrator/autonomy-runtime.js`, `environment/control/capability-handshake.js`, `environment/tests/cli/research-loop.test.js`, `environment/tests/cli/bin-vre-phase9-stubs.test.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-counts.js`, `environment/tests/ci/validate-runtime-contracts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/cli/research-loop.test.js` (8/8 pass: no active objective pointer -> structured failure; no sanctioned next slice -> `E_LLM_REASONING_REQUIRED` blocker; one bounded safe slice; budget exhausted pause; duplicate wake id no-op; stale wake reclaim event; crash-before-final-snapshot recovery; semantic drift pause); `node --test environment/tests/control/capability-handshake.test.js environment/tests/cli/bin-vre-phase9-stubs.test.js` (14/14 pass, including truthful `research-loop` executable/missing-surface state); `node --test environment/tests/cli/*.test.js` (49/49 pass); `npm run build:surface-index` (38 total surfaces, including new `autonomy-runtime` orchestrator-surface); `npm run test:phase9` (197 pass, 0 fail, 1 skipped) | verified | Wave 4 `T4.1`. `environment/orchestrator/autonomy-runtime.js` is the first real unattended loop controller for Phase 9 v1: it claims the wake lease under the active objective pointer lock, no-ops duplicate wake ids, reclaims stale leases with `stale-wake-lease-reclaimed`, runs a deterministic strategic checkpoint before spending the slice, derives the next sanctioned analysis candidate mechanically from durable state, blocks with `E_LLM_REASONING_REQUIRED` instead of inventing a new plan when rule-only cannot derive a next slice, appends `queue.jsonl` running/final records as write-ahead state, executes at most one sanctioned `run-analysis` slice, syncs memory before the final snapshot, and repairs `resume-snapshot.json` from queue/event durability if the prior process crashed after queue/event append but before the final snapshot rewrite. Same-patch truthfulness fallout promotes `research-loop` from Wave 0 stub to a real executable command in the capability handshake and CLI-stub test suite, regenerates the machine inventory with `autonomy-runtime` as a first-class orchestrator surface, and keeps the undocumented-command degraded warning honest because `commands/research-loop.md` still does not exist. Out of scope, deliberately NOT closed here: (a) Windows scheduler install/status/doctor/remove remain on their own Wave 4 tasks and are still stub-only; (b) the broader morning artifact journey and machine wake registration remain later Wave 4 slices; (c) this row lands the first executable semantic-drift gate for T4.1, but the broader Wave 4 semantic-drift cluster remains `in-progress` until the dedicated T4.6 slice closes it. |
| 076 | 2026-04-23 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Close the seq-075 claim-without-pin gap for two explicit frozen T4.1 behaviors: the final-quarter strategic relevance checkpoint trigger (`phase='final-quarter'` before spending the last 25% of the iteration budget) and the memory-sync-skipped write-back into both the `loop-iteration` event payload and the final resume snapshot `notes` | `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/cli/research-loop.test.js` (10/10 pass post-Round-61, +2 regression tests: final-quarter checkpoint phase drifts after pre-slice aligned -> `SEMANTIC_DRIFT_DETECTED` with `phaseLog === [pre-slice, final-quarter]`; injected `syncMemory` that throws records `memorySync.status=skipped` + `reason` in the loop-iteration event and surfaces `Memory sync skipped: <reason>` in the resume snapshot notes); `npm run test:phase9` (199 pass post-Round-61, 0 fail, 1 skipped, +2 over the T4.1 197 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 61 delta including spec-side paths) | verified | Round 61 adversarial closure on top of seq 075. Deep-read review of `autonomy-runtime.js` found two implemented-but-unpinned claims: the final-quarter checkpoint branch and the memory-sync-skipped propagation path. Closure added two regression tests and did not change runtime behavior. Documentary integrity note: this row was malformed in the committed markdown table after the original Round 61 append; Round 63 repairs the row text in place as a transparent ledger-integrity exception, without changing the underlying Round 61 substance. |
| 077 | 2026-04-23 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Second-pass deep adversarial closure on top of seq 075/076: close four additional claim-without-pin gaps in the T4.1 autonomy runtime that the first-pass Round 61 review did not surface (onBudgetExhausted=block default branch, onBudgetExhausted=stop terminal branch, wake-lease-still-active no-op reason, and E_OBJECTIVE_ID_MISMATCH cross-objective safety guard) | `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/cli/research-loop.test.js` (14/14 pass post-Round-62, +4 regression tests: onBudgetExhausted=block writes BLOCKER.flag + blocker-open event with E_BUDGET_EXHAUSTED; onBudgetExhausted=stop takes the terminal stopObjective path with disposition=stop event; contending wake id against an unexpired lease owned by a different session returns status=no-op + reason=wake-lease-still-active with zero queue mutation; requesting --objective on an ID different from the active pointer fails closed with E_OBJECTIVE_ID_MISMATCH and emits activeObjectiveId + objectiveId in the structured payload); `npm run test:phase9` (203 pass post-Round-62, 0 fail, 1 skipped, +4 over the Round-61 199 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 62 delta) | verified | Round 62 adversarial closure on top of seq 075/076. After Round 61 closed two claim-without-pin gaps (final-quarter checkpoint + memory-sync-skipped notes), a second pressure pass enumerated EVERY stopReason path and EVERY noOpReason in autonomy-runtime.js and found four more explicit runtime behaviors still without dedicated coverage: (1) `applyBudgetStopCondition` branch for `onBudgetExhausted === "block"` (the DEFAULT value in the fixture `valid-active.json`) wrote BLOCKER.flag + blocker-open event without any test exercising it; (2) the TERMINAL branch `onBudgetExhausted === "stop"` called `stopObjective()` and emitted `disposition: "stop"` event without coverage; (3) `resolveWakeRequest` had two noOp branches (`duplicate-wake-id`, already pinned, and `wake-lease-still-active` for a different wakeId contending against an unexpired lease owned by another session, not pinned); (4) the defensive `E_OBJECTIVE_ID_MISMATCH` check at both runtime layer 394 and CLI entry 822 prevented cross-objective slice execution but was never exercised. None of these required runtime changes; all four tests confirm existing runtime behavior with assertions that pin it against silent regressions. No new runtime surface; no new schema; no in-place edit of earlier seq rows; same `W4-AUTONOMY-RUNTIME-LOOP` featureId used to satisfy the orchestrator-surface orphan check. Transparent out-of-scope follow-ups: other stopConditions fields (`onBlocker`, `onAmbiguousEvidence`, `onMissingInput`, `onFailedGate`, `onHumanDecisionNeeded`) are schema-declared but unenforced in the T4.1 runtime and remain owned by Wave 4.5 broader hardening; the capability-handshake artifact is loaded but not re-persisted after a slice, unlike `objective start/pause/stop`, and remains a Wave 4 question rather than a T4.1 gap. |
| 078 | 2026-04-23 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Land frozen `T4.2` research-loop CLI mode semantics: `--mode` is now a durable-state assertion instead of a silent parse-and-drop flag, `--resume` reads the stored `objective.runtimeMode` instead of caller mood, interactive human-started slices remain allowed, unattended-batch still blocks instead of asking inline, and resume-only objectives refuse execution even if the caller passes a different CLI mode | `environment/orchestrator/autonomy-runtime.js`, `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/cli/research-loop.test.js` (17/17 pass post-Round-63, +3 T4.2 regressions: interactive mode happy path, unattended-batch empty-queue blocker via explicit `--mode unattended-batch`, and `--resume --mode unattended-batch` still failing with `E_RUNTIME_MODE_EXECUTION_FORBIDDEN` when the stored mode is `resume-only`; plus one non-frozen but necessary drift-closure regression proving `--mode` mismatch fails closed with `PHASE9_USAGE`); `node --test environment/tests/cli/*.test.js` (58/58 pass); `npm run test:phase9` (206 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 63 delta including spec-side paths) | verified | Wave 4 `T4.2`. Adversarial review of the post-Round-62 state found one real CLI drift: `bin/vre` parsed `research-loop --mode ...`, but `autonomy-runtime.js` silently ignored `options.mode`, so the operator-facing flag existed as a deceptive parse-and-drop surface. Closure is deliberately narrow and follows file 11's durable-state rule: `objective.runtimeMode` remains authoritative, `--resume` always uses the stored mode, and non-resume invocations may only ASSERT the stored mode through `--mode`; a mismatch now fails closed with structured `PHASE9_USAGE` instead of pretending the CLI flag had meaning. No new runtime surface, no new schema, and no change to T4.1 slice semantics. Same-pass documentary cleanup repairs the malformed committed `seq 076` markdown row so the append-only ledger remains human-readable; this is recorded transparently as a ledger-integrity exception, not as a precedent for rewriting historical substance. |
| 079 | 2026-04-23 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Close the T4.2 parse-and-drop drift on `--stop-on-blocker`: bin/vre parsed the flag and extracted it into `stopOnBlocker` as a boolean, but `autonomy-runtime.js` never read `options.stopOnBlocker`, so the spec-promised CLI surface (blueprint file 13 `13-user-journey-and-cli-surface.md:140`) was deceptive just like `--mode` was before Round 63 closed it | `bin/vre` | none | `node --test environment/tests/cli/research-loop.test.js` (17/17 pass post-Round-64; no test regression, no runtime behavior change); `npm run test:phase9` (206 pass, 0 fail, 1 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 64 delta) | verified | Round 64 adversarial closure on top of seq 078. Third deep pressure pass on T4.2 landing applied the same enumeration we used for Round 62 (every CLI flag parsed by bin/vre must either be used by the runtime, wired to a defined semantic, or explicitly rejected/deferred). Found that `--stop-on-blocker` was the only remaining parse-and-drop flag: bin/vre lines 869-871 extracted `stopOnBlocker: bool` from `parsed.options` and passed it into `runResearchLoopCommand`, but `grep -rn stopOnBlocker environment/ bin/` showed ZERO consumers in the runtime. The spec file 13 example at line 140 shows `--stop-on-blocker` as a legitimate operator flag combined with `--max-iterations 1 --max-wall-seconds 1800 --mode unattended-batch` for bounded overnight probes. Round 64 closure: remove the dead `stopOnBlocker` extraction so the options object no longer carries a field that the runtime ignores. This does not remove the CLI surface (`parseLongOptions` is permissive and still accepts any `--foo` without validation), but it converts the drift from "silently extracted and silently dropped" into "silently accepted by parser and not propagated to the runtime", which is a strictly smaller deception vector. The spec-promised multi-slice semantic of `--stop-on-blocker` (halt the loop before the next slice when a blocker has been recorded) belongs to Wave 4.5+ because T4.1/T4.2 v1 run at most ONE slice per invocation and therefore always halt after any single-slice blocker. Transparent out-of-scope follow-ups restated: (a) `onBlocker`, `onAmbiguousEvidence`, `onMissingInput`, `onFailedGate`, `onHumanDecisionNeeded` stopConditions remain unenforced in the runtime and are owned by Wave 4.5; (b) strict CLI option validation (rejecting unknown long options instead of silently accepting them via the permissive `parseLongOptions`) is a broader CLI surface question rather than a T4.2 gap and is tracked for later. |
| 080 | 2026-04-23 | 4 | W4-SCHEDULER-REGISTRATION-CLI | Land frozen `T4.2b` machine-level scheduler registration: `scheduler install/status/doctor/remove` and `objective doctor` are now real, Windows Task Scheduler definitions are deterministic and heartbeat-compatible, install fails closed without leaving a falsely armed objective, status stops fabricating `boundObjectiveId` when no task exists, and the capability-handshake `operatorSurface` truthfully lists the canonical file-13 operator/doctor commands even after they become executable | `bin/vre`, `environment/orchestrator/windows-task-scheduler.js`, `environment/control/capability-handshake.js`, `environment/tests/cli/scheduler-cli.test.js`, `environment/tests/control/windows-task-scheduler.test.js`, `environment/tests/control/capability-handshake.test.js`, `environment/tests/control/capability-handshake-drift.test.js`, `environment/tests/fixtures/phase9/capability-handshake/valid-full.json`, `environment/tests/fixtures/phase9/capability-handshake/valid-degraded-no-kernel.json`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/control/windows-task-scheduler.test.js` (5/5 pass: deterministic install/status, conditional doctor + idempotent remove, manual `schtasks /Run`-style heartbeat probe path, post-registration contract-failure cleanup preserving `wakeSourceId`, and `objective doctor` delegating scheduler doctor); `node --test environment/tests/cli/scheduler-cli.test.js` (1 pass, 0 fail, 4 skipped on the local Windows host because deterministic unsupported-path coverage runs only on non-Windows CI); `node --test environment/tests/control/capability-handshake.test.js environment/tests/control/capability-handshake-drift.test.js` (16/16 pass, pinning truthful `operatorSurface` plus live docs-vs-dispatch alignment after scheduler/objective-doctor become executable); `node --test environment/tests/cli/bin-vre-phase9-stubs.test.js` (7/7 pass, leaving only `capabilities doctor` as the remaining doctor stub); `npm run build:surface-index` (39 total surfaces, including `windows-task-scheduler`); `npm run test:phase9` (212 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 65 delta including spec-side paths) | verified | Wave 4 `T4.2b` implementation landing. The new `environment/orchestrator/windows-task-scheduler.js` provides the narrow Windows Task Scheduler adapter frozen by file 11/file 13: `scheduler install` creates or updates one deterministic machine-level task that runs `node bin/vre research-loop --objective OBJ-... --heartbeat --wake-id <taskName> --json`; `scheduler status` returns structured machine-readable state without inventing a bound objective when no task exists; `scheduler doctor` probes host support and task contract honestly; `scheduler remove` is idempotent; and `objective doctor` now delegates scheduler preconditions when `wakeOwner=windows-task-scheduler`. Same-pass adversarial closure fixed four real drifts before recording this row: (1) the original task definition omitted the runtime-required `--wake-id` and the frozen contract's `--json`; (2) `scheduler install` wrote `wakeSourceId` before proving the registration was valid, which could leave the objective falsely armed after a contract failure; (3) `scheduler status` fabricated `boundObjectiveId` from the requested objective even when no task existed; and (4) `capability-handshake` incorrectly removed now-executable operator/doctor commands from `vre.operatorSurface`, contradicting spec file 03/file 13. Important acceptance note: the frozen non-CI manual wake test (real sleeping workstation wakes and emits exactly one heartbeat event) has NOT been executed in this row, so the higher-level scheduler contract cluster in the spec-side status ledger remains `in-progress` even though the code, CLI, inventory, and CI contract tests for T4.2b are now landed. |
| 081 | 2026-04-23 | 4 | W4-SCHEDULER-REGISTRATION-CLI | Close a T4.2b wake-lease correctness drift on top of seq 080: the registered task no longer reuses a constant `wakeId` derived from the deterministic task name, and the runtime now materializes a fresh machine-generated wake id per heartbeat invocation from an explicit `--wake-id auto` sentinel | `bin/vre`, `environment/orchestrator/windows-task-scheduler.js`, `environment/orchestrator/autonomy-runtime.js`, `environment/tests/control/windows-task-scheduler.test.js`, `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/control/windows-task-scheduler.test.js` (5/5 pass, with the manual `schtasks /Run`-style probe now proving the scheduler task does NOT echo the deterministic task name and yields distinct generated wake ids across two invocations); `node --test environment/tests/cli/research-loop.test.js` (18/18 pass, +1 regression proving `research-loop --heartbeat --wake-id auto` replaces the sentinel with a generated `wake-*` id before claiming the lease and persists that generated id into both `currentWakeLease` and the final resume snapshot); `npm run test:phase9` (213 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 66 delta including spec-side paths) | verified | Round 66 adversarial closure on top of seq 080. Deep read of the scheduler/runtime interaction found one more real drift hiding behind otherwise-green T4.2b tests: `expectedTaskDefinition()` reused the deterministic task name as the scheduled `--wake-id`, but `autonomy-runtime.js` treats an identical heartbeat wake id as `duplicate-wake-id` before checking lease expiry. That meant a correctly registered scheduler task could degrade into a permanent duplicate-wake no-op after the first successful run. Closure is deliberately narrow: the registered task keeps the deterministic task name for installation identity and `wakeSourceId`, but now passes `--wake-id auto`; `research-loop` maps the sentinel to a fresh generated `wake-*` value for real runtime execution, and the dry-run heartbeat probe path does the same so `scheduler doctor` still validates the same invocation shape honestly. This row corrects the seq-080 wake-id assumption without rewriting the earlier row in place: seq 080 remains the implementation landing, seq 081 records the wake-id correctness fix that makes the scheduler contract safe against permanent duplicate-wake suppression. Manual sleeping-workstation wake acceptance is still pending, so the higher-level spec-side scheduler cluster remains `in-progress` even after this fix. |
| 082 | 2026-04-24 | 4 | W4-SCHEDULER-REGISTRATION-CLI | Close the seq-080/seq-081 claim-without-pin gap inside `objectiveDoctorCommand`: every guard branch before the scheduler doctor delegation now has a dedicated regression test, so a future refactor cannot silently drop any of the eight named errors raised by the `objective doctor` path | `environment/tests/control/windows-task-scheduler.test.js` | none | `node --test environment/tests/control/windows-task-scheduler.test.js` (13/13 pass post-Round-67, +8 new regression tests over the seq-080/seq-081 5/5 baseline: `E_OBJECTIVE_NOT_FOUND`, `E_ACTIVE_OBJECTIVE_POINTER_MISSING`, `E_OBJECTIVE_ID_MISMATCH`, `E_OBJECTIVE_STATE_INVALID`, `E_RUNTIME_MODE_UNSUPPORTED`, `E_REASONING_MODE_UNSUPPORTED`, `E_OBJECTIVE_ARTIFACT_PATH_MISSING`, `E_STATE_CONFLICT`); RED-first proof on `E_OBJECTIVE_ARTIFACT_PATH_MISSING` branch (observed actual `E_WRONG_CODE_FOR_RED` before revert) confirms the test pins the exact code and would catch a silent regression; `npm run test:phase9` (221 pass, 0 fail, 5 skipped, +8 over the Round-66 213 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 67 delta including spec-side paths) | verified | Round 67 adversarial closure on top of seq 080 + seq 081. Deep-read enumeration of every `code: 'E_*'` raised inside `environment/orchestrator/windows-task-scheduler.js` (applying the same Round 62 methodology that closed four T4.1 autonomy-runtime claim-without-pin gaps) found that the `objectiveDoctorCommand` function raises eight distinct named error codes before it delegates to `schedulerDoctorCommand`, but only the delegation happy-path was pinned by the seq-080 test suite (`objective doctor fails when scheduler doctor reports unsupported` at `windows-task-scheduler.test.js:321`). Adversarial cross-check of every test file in the suite confirmed that four of those codes (`E_OBJECTIVE_NOT_FOUND`, `E_RUNTIME_MODE_UNSUPPORTED`, `E_OBJECTIVE_ARTIFACT_PATH_MISSING`, plus the `objective doctor` call-site for `E_STATE_CONFLICT`) had ZERO test anywhere, while the other four (`E_ACTIVE_OBJECTIVE_POINTER_MISSING`, `E_OBJECTIVE_ID_MISMATCH`, `E_OBJECTIVE_STATE_INVALID`, `E_REASONING_MODE_UNSUPPORTED`) were pinned only for OTHER call-sites (research-loop, objective CLI, bin-vre-phase9-stubs) and left the objective-doctor branch unpinned. This is the same deception vector that Round 62 closed for `autonomy-runtime.js`: behaviors implemented correctly but never pinned by a dedicated test, so the seq-080 ledger claim "`objective doctor` is now real" was technically truthful for the delegation happy-path but silently fragile for the eight guard branches in front of it. Round 67 closure is narrow and test-only: eight new regressions appended after the existing `objective doctor fails when scheduler doctor reports unsupported` test, each invoking `objectiveDoctorCommand` with the minimum mutation required to reach ONE specific branch (bypass via `deps.readObjectiveRecord` / `deps.validateObjectiveRecord` / `deps.readActiveObjectivePointer` / `deps.readObjectiveEvents` injections for defensive paths that the schema validator would otherwise block, plus `seedActiveObjective` overrides for the schema-compatible branches). No runtime change, no new schema, no in-place edit of seq 080 or seq 081; same `W4-SCHEDULER-REGISTRATION-CLI` featureId used to satisfy the orchestrator-surface orphan check. Transparent out-of-scope follow-ups NOT closed here: (a) `validateWakeOwner` (lines 479-488) raises `PHASE9_USAGE` for objectives without `wakeOwner=windows-task-scheduler` from all four scheduler commands but has no dedicated test either; it is a single-function gap (one call-site per command) that would benefit from one regression per caller but is strictly smaller than the objective-doctor cluster closed here and can be addressed alongside T4.3 or a dedicated hygiene pass; (b) the frozen non-CI manual wake test (real sleeping workstation wakes and emits exactly one heartbeat event) is still pending and therefore the higher-level scheduler contract cluster on the spec-side status ledger remains `in-progress`; (c) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning in vibe-science CI predates Wave 4 and remains a dedicated cleanup candidate outside the freeze corridor. |
| 083 | 2026-04-24 | 4 | W4-SCHEDULER-REGISTRATION-CLI | Close 11 residual T4.2b claim-without-pin gaps that Round 67 seq 082 transparently deferred or missed: 4 `validateWakeOwner` PHASE9_USAGE call-sites (one per scheduler command), 3 symmetric `--objective`-missing PHASE9_USAGE paths (status/doctor/remove; install was already pinned), and 4 positional-argument PHASE9_USAGE paths (install/status/doctor/remove). Seq 082 explicitly deferred `validateWakeOwner` as "a smaller single-function gap tracked for post-T4.3 hygiene"; pressure re-review confirmed that deferral was wrong because the guard is invoked from all four scheduler commands and has no dedicated test at any of them | `environment/tests/control/windows-task-scheduler.test.js`, `environment/tests/cli/scheduler-cli.test.js` | none | `node --test environment/tests/control/windows-task-scheduler.test.js` (17/17 pass post-Round-68, +4 new `validateWakeOwner` regressions over the Round-67 13/13 baseline: `scheduler install`/`status`/`doctor`/`remove` each reject `wakeOwner=manual`/`codex-app`/`vre-automation`/`external-runner` with structured `PHASE9_USAGE` and the canonical "requires objective OBJ-001 to declare wakeOwner=windows-task-scheduler" message); `node --test environment/tests/cli/scheduler-cli.test.js` (12 pass / 4 skipped on the local Windows host, +7 new CLI PHASE9_USAGE regressions over the Round-67 5 pass / 4 skipped baseline: 3 `--objective`-missing paths for status/doctor/remove plus 4 unexpected-positional-argument paths for install/status/doctor/remove); `node --test environment/tests/control/*.test.js` (117/117 pass post-Round-68, +4 over the Round-67 113/113); `npm run test:phase9` (232 pass, 0 fail, 5 skipped, +11 over the Round-67 221 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 68 delta including spec-side paths) | verified | Round 68 adversarial closure on top of seq 080 / 081 / 082. Pressure re-review after seq 082 applied the same Round 62 methodology yet again and found three residual claim-without-pin classes still open: (1) `validateWakeOwner` (windows-task-scheduler.js:479-488) raises `PHASE9_USAGE` from all four scheduler commands when the objective declares `wakePolicy.wakeOwner !== "windows-task-scheduler"`; seq 082 flagged the gap and deferred it, which retrospectively was wrong — the guard is a single function but its four call-sites each deserve a dedicated regression; (2) the bin/vre CLI dispatch (lines 978-1077) enforces `requiredStringOption(parsed, "objective", ...)` for all four scheduler commands but only `scheduler install` had a `--objective` missing regression (scheduler-cli.test.js:69); (3) the bin/vre CLI dispatch enforces `parsed.positionals.length > 0 -> PHASE9_USAGE` for all four commands but NO command had a positional-argument regression. Round 68 closure is narrow and test-only: 11 new regressions appended (4 in control test, 7 in CLI test). Each test uses the minimum mutation required to reach one specific path — `wakePolicy.wakeOwner` override via `seedActiveObjective` for the control branch, `runVre` with `--objective` omitted or with an explicit `unexpected-positional-arg` for the CLI branch. No runtime change, no new schema, no in-place edit of earlier seq rows; same `W4-SCHEDULER-REGISTRATION-CLI` feature id used to keep the orchestrator-surface orphan check aligned. Out-of-scope follow-ups transparently restated: (a) the frozen non-CI manual wake acceptance test (real sleeping workstation wakes and emits exactly one heartbeat event) is still pending, so the higher-level scheduler contract cluster on the spec-side status ledger remains `in-progress`; (b) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning in vibe-science CI predates Wave 4 and remains outside the freeze corridor. |
| 084 | 2026-04-24 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Close a cross-feature test-infrastructure time-drift flakiness surfaced while verifying seq 083: `environment/tests/cli/research-loop.test.js` had hard-coded the seed objective's `createdAt` to `2026-04-23T20:00:00Z`, but autonomy-runtime.js:943-952 computes `wallSecondsConsumed = Date.parse(writtenAt) - Date.parse(objectiveRecord.createdAt)` and blocks the slice when it exceeds `budget.maxWallSeconds` (28800s = 8h); once the system clock rolled past 2026-04-24T04:00Z every bounded-slice research-loop test started returning `status: 'blocked'` instead of the expected `'slice-complete'` / `'E_LLM_REASONING_REQUIRED'` | `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/cli/research-loop.test.js` (18/18 pass post-Round-68, previously 9/18 failing with `actual: 'blocked', expected: 'slice-complete'` / `actual: 'budget-exhausted', expected: 'E_LLM_REASONING_REQUIRED'` under any wall-clock past 2026-04-24T04:00Z); RED-first evidence: the nine failures reproduced deterministically against the seq-082 feb1054 code after the system clock advanced and disappeared once the fixture timestamp became relative; `npm run test:phase9` (232 pass, 0 fail, 5 skipped, same +11 delta as seq 083 plus the 9 research-loop tests back to green); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 68 delta) | verified | Round 68 test-fixture hygiene. The fix narrows `seedObjective` to seed a relative `createdAt`/`lastUpdatedAt` (now - 60min / now - 30min) only when the caller does not pin them, so tests stay idempotent regardless of the wall-clock date and callers that still need a specific fixture timestamp continue to receive it. No runtime change, no schema change, no in-place edit of earlier seq rows. This row transparently documents that Codex's Round 65/66 landing was verified on the same calendar day (2026-04-23) and therefore never hit the budget-exhausted branch before the system clock rolled over; the same flakiness would have caught CI on the next scheduled nightly run even without Round 68's new tests. Out-of-scope follow-up: the other hardcoded `'2026-04-23T...'` timestamps in research-loop.test.js (manifest createdAt at line 214, binding updatedAt at line 232, several `setPointerWakeLease` timestamps around lines 596/819/873) do not feed the wallSecondsConsumed computation and therefore do not cause today's flakiness; they can be migrated to relative timestamps in a dedicated hygiene pass when `T4.5.4` test-fixture refactoring is in scope. |
| 085 | 2026-04-24 | 0 | W0-PHASE9-TEST-ENTRYPOINT | Close a Round-68 false-green in the Phase 9 validator harness: repo-root `.tmp-*` CLI fixture projects were still being traversed by the shared `collectFiles('.')` walker, so `validate-no-personal-paths` could crash with `ENOENT` on disappearing transient directories even when the checked-in repo state was clean; the walker now skips repo-root `.tmp-*` fixture trees by default and treats vanished transient directories as ignorable, and a new Phase 9 CI regression test pins both the walker skip and validator immunity while the named `test:phase9` entrypoint and runtime-contract pinning include the new test file | `environment/tests/ci/_helpers.js`, `environment/tests/ci/validate-no-personal-paths.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/phase9-surface-index.test.js`, `package.json` | none | `node --test environment/tests/ci/validate-no-personal-paths.test.js` (2/2 pass, pinning both repo-root `.tmp-*` skip and `validate-no-personal-paths` immunity to forbidden content inside temp fixture trees); `npm run validate` (green, all 12 validators, including the previously failing `validate-no-personal-paths` path); `npm run test:phase9` (234 pass, 0 fail, 5 skipped); `npm run check` (718 pass, 0 fail, 9 skipped); `npm run build:surface-index` (green; 39 surfaces unchanged) | verified | Round 69 adversarial closure on top of seq 083-084. Independent rerun of the supposedly-green Round 68 state found a real residual drift: `npm run validate` could fail locally with `ENOENT ... .tmp-vre-objective-cli-no-wake-.../environment/schemas` because CLI fixture helpers create temporary projects inside the repo root (`environment/tests/cli/_fixture.js` uses `mkdtemp(path.join(repoRoot, '.tmp-...'))`), but the shared validator walker still descended into those transient trees. This made the validator harness sensitive to temp-fixture cleanup timing rather than only to checked-in repo content. Closure is deliberately narrow and test-driven: the walker now skips `.tmp-*` directories, returns cleanly if a transient directory disappears during traversal, and the new test is wired into `test:phase9` plus `validate-runtime-contracts` and the surface-index fixture package script so the fix cannot silently fall out of the named Phase 9 entrypoint. No new runtime surface, no schema change, no machine surface-inventory delta, and no change to Wave 4 runtime semantics. |
| 086 | 2026-04-24 | 4 | W4-QUEUE-AND-TASK-SELECTION | Land frozen T4.3 queue and task-selection semantics: a reviewed objective-local `queue.jsonl` adapter now validates durable queue lines, `research-loop` blocks disallowed task kinds, refuses `queue.jsonl` terminal rows without matching terminal objective events, detects `incomplete-at-crash` from a non-terminal task intent, writes immutable digest artifacts plus `handoffs.jsonl` evidence, and keeps queue work tied to objective/session/wake lineage without inventing a new schema family outside the frozen scope | `environment/orchestrator/autonomy-runtime.js`, `environment/orchestrator/queue-adapter.js`, `environment/tests/cli/research-loop.test.js`, `environment/tests/control/queue-adapter.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/control/queue-adapter.test.js` (3/3 pass, pinning fresh-process reconstruction of pending/completed queue state plus required-field and objectiveId fail-closed validation on `queue.jsonl`); `node --test environment/tests/cli/research-loop.test.js` (22/22 pass, including `E_TASK_KIND_NOT_ALLOWED`, field-by-field `queue.jsonl` binding for `objectiveId` / `taskId` / `taskKind` / `taskAttemptId` / `sessionId` / `wakeId` / source/result artifact paths / `resumeCursor`, immutable `digest-<ts>.md` + `digest-latest.md` dual-write equivalence, explicit `failed` terminal queue result persistence, `E_QUEUE_TERMINAL_WITHOUT_EVENT` when a prior wake left a terminal queue row without the matching terminal objective event, and `E_TASK_INCOMPLETE_AT_CRASH` with handoff write + no duplicate rerun for a non-idempotent running task); `npm run build:surface-index` (40 total surfaces, including the new `queue-adapter` orchestrator surface under `W4-QUEUE-AND-TASK-SELECTION`); `npm run test:phase9` (241 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators); `npm run check` (725 pass, 0 fail, 9 skipped); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on the full T4.3 delta including spec-side paths) | verified | Round 70 implements frozen T4.3 after a deep adversarial read found the contract only partially present in seq 075/078/081 runtime work: queue state lived inside `autonomy-runtime.js` without a reviewed adapter surface, objective-local queue lines were not independently validated, there was no fail-closed `E_QUEUE_TERMINAL_WITHOUT_EVENT` branch for a queue terminal row missing the matching terminal objective event, there was no `incomplete-at-crash` blocker/handoff path for a dangling running intent, and the acceptance-evidence artifacts named by file 08 (`queue.jsonl`, `handoffs.jsonl`, immutable digest files) were not being written as part of the real bounded-slice flow. Same-round deep adversarial follow-up then found four more claim-without-pin gaps even after the first green local pass: the required queue-line field binding itself was not asserted field-by-field, the deterministic `resumeCursor` progression was not pinned, the non-success terminal result path (`failed` / `interrupted`) was implemented but untested, and the digest dual-write contract was only lightly covered. Closure stays deliberately scope-tight and plan-faithful: `environment/orchestrator/queue-adapter.js` is the new durable read/write/derive-state adapter for `.vibe-science-environment/objectives/<OBJ-ID>/queue.jsonl`; `autonomy-runtime.js` now uses it to enforce the write-ahead queue path, summarize `queueVisibility`, refuse terminal queue evidence without the matching terminal event, block and emit a handoff/digest on `incomplete-at-crash`, and keep the older crash-repair path truthful by moving the repair regression to `afterLoopEventPersisted` (terminal event already durable) instead of the previously misleading `afterQueueResultPersisted` seam. The strengthened `research-loop.test.js` suite now pins all four of those queue/digest claims explicitly. No new schema is introduced: the adapter validates the reviewed queue shape directly rather than inventing a parallel `phase9.queue-record.v1` family outside the freeze corridor. Honest residuals remain: this lands the queue/handoff/digest evidence named by frozen T4.3, but the broader morning-artifact and immutable acceptance-bundle contract owned by T4.4 / later Wave 4-6 rows is still open. |
| 087 | 2026-04-24 | 4 | W4-QUEUE-AND-TASK-SELECTION | Close two residual T4.3 claim-without-pin gaps on top of seq 086: (a) three defensive validator branches in `queue-adapter.js` (status enum membership, resumeCursor object shape, strictly-monotonic recordSeq) that were enforced in code but only implicit in the seq-086 test suite; (b) the sessionId/wakeId/objectiveId lineage assertion on the post-crash queue row, which spec-side row 95 explicitly cites but the incomplete-at-crash test only proved transitively through "no new record appended" | `environment/tests/control/queue-adapter.test.js`, `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/control/queue-adapter.test.js` (6/6 pass post-Round-71, +3 new regression tests over the seq-086 3/3 baseline: `status must be one of`, `resumeCursor must be an object`, `recordSeq must be strictly monotonic`); `node --test environment/tests/cli/research-loop.test.js` (22/22 pass, same count as Round 70 — the incomplete-at-crash test now additionally asserts `queueRecords[0].sessionId === 'sess-incomplete-crash'`, `queueRecords[0].wakeId === 'WAKE-INCOMPLETE-CRASH'`, and `queueRecords[0].objectiveId === context.objectiveId`, pinning spec-side row 95 lineage claim explicitly); RED-first proof: replacing `!OBJECTIVE_QUEUE_STATUSES.has(record.status)` with `false` in queue-adapter.js triggered the new status-enum regression with `expected: /status must be one of/u, operator: 'rejects'` before revert, confirming the test pins the exact invariant; `npm run test:phase9` (244 pass, 0 fail, 5 skipped, +3 over the Round-70 241 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 71 delta including spec-side paths) | verified | Round 71 adversarial closure on top of seq 086. Deep cross-check of the frozen T4.3 contract (plan file 05 lines 170-223) against the Round-70 landing surfaced two distinct drift classes. First, `assertValidObjectiveQueueRecord` (queue-adapter.js:72-109) enforces ~15 defensive branches but seq 086 only pinned 2 (missing taskId + mismatched objectiveId). Round 71 adds regressions for the three branches whose silent regression would cause the hardest downstream failures — a zombie `status` value accepted and propagated into `deriveObjectiveQueueState` would poison queueVisibility; an invalid `resumeCursor` shape would break the resume path of Wave 4 loop consumers; a backward-moving `recordSeq` would break the append-only monotonic invariant that write-ahead recovery depends on. Second, spec-side row 95 (`11-autonomy-runtime-and-scheduler-contract.md` Wake-lease cluster) claims the incomplete-at-crash path "remains bound to the original wake/session lineage instead of duplicating the running attempt automatically"; the seq-086 test proved this transitively (no new record appended means the existing record is the original by construction) but did not assert the sessionId/wakeId/objectiveId fields directly. Round 71 pins them explicitly. No runtime change, no new schema, no in-place edit of earlier seq rows; same `W4-QUEUE-AND-TASK-SELECTION` feature id used. Out-of-scope follow-ups transparently NOT closed here: (a) spec plan file 05 line 193 says "the runtime must not blindly re-run it unless the task kind is declared idempotent in the task registry or the resume policy says to ask the operator"; `task-registry-entry.schema.json` currently has no `idempotent` field, `analysis-execution-run` is not in the task registry at all, and the autonomy runtime never consults the registry for T4.3 task kinds. The current implementation therefore always takes the SAFE DEFAULT branch (handoff for every non-idempotent incomplete-at-crash); the conditional idempotent-auto-rerun and explicit resume-policy branches described by spec are currently unreachable dead code paths that would require a schema extension, task-registry entry for `analysis-execution-run`, and runtime wiring between `autonomy-runtime.js` and `task-registry.js` — all larger than T4.3 scope and more naturally in the T4.5 external-wake / broader registry integration; (b) the remaining `assertValidObjectiveQueueRecord` branches (`taskKind`, `taskAttemptId`, `sessionId`, `wakeId`, `createdAt`, `updatedAt`, `sourceArtifactPaths`, `resultArtifactPaths`, `handoffId`, `recordSeq positive-integer`) follow the same `assertNonEmptyString` / `assertIsoDateTime` / `assertStringArray` pattern already pinned by the taskId regression from seq 086, so individual regressions for each would be pure symmetry coverage and can migrate when `T4.5.4` test-fixture refactoring is in scope; (c) the frozen non-CI sleeping-workstation wake acceptance test for the scheduler cluster is still pending and keeps the spec-side scheduler row `in-progress`; (d) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning in vibe-science CI predates Wave 4. |
| 088 | 2026-04-24 | 4 | W4-QUEUE-AND-TASK-SELECTION | Pure symmetry coverage hardening for the 10 `assertValidObjectiveQueueRecord` defensive validator branches that Round 71 seq 087 explicitly deferred as "pure symmetry following the taskId pattern already pinned by seq 086", plus one symmetric happy-path `loopEvent.payload.status` assertion that Round 71 deferred because the failed-case counterpart was already pinned at `research-loop.test.js:683-684`. This row is deliberately labeled as symmetry-only — it does NOT close a claim-without-pin gap from any ledger row, it prevents silent regression across every individual validator path so a future refactor cannot silently drop one guard without at least one test catching it | `environment/tests/control/queue-adapter.test.js`, `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/control/queue-adapter.test.js` (11/11 pass post-Round-72, +5 new table-driven regressions over the Round-71 6/6 baseline covering 10 distinct validator branches: 4 required-non-empty-string fields (`taskKind`, `taskAttemptId`, `sessionId`, `wakeId`), 2 ISO-date-time fields (`createdAt`, `updatedAt`), 2 string-array fields (`sourceArtifactPaths`, `resultArtifactPaths`), the `handoffId` non-null empty-string guard, and the `recordSeq` positive-integer guard); `node --test environment/tests/cli/research-loop.test.js` (22/22 pass, same count as Round 71 — added 2 event-level assertions inside the existing happy-path test: `loopEvent.payload.status === 'complete'` and `loopEvent.payload.resultCode === null`, symmetric to the failed-case assertions already at lines 683-684); RED-first proof: commenting out `assertNonEmptyString(record.taskKind, 'taskKind')` in queue-adapter.js triggered the Round-72 `missing each individually required string field` regression with `expected: /taskKind must be a non-empty string/u` before revert, confirming the table-driven test pins the exact error for every listed field; `npm run test:phase9` (249 pass, 0 fail, 5 skipped, +5 over the Round-71 244 baseline); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 72 delta including spec-side paths) | verified | Round 72 symmetry-only coverage. Intentionally NOT labeled as drift closure: every previously-unpinned branch follows the same assertion pattern already pinned by the Round-70 taskId regression (`assertNonEmptyString`), the Round-71 status-enum regression (`OBJECTIVE_QUEUE_STATUSES.has`), or the Round-71 resumeCursor-shape regression; the gap was pure coverage, not a spec or ledger violation. Round 72 adds 5 compact table-driven regressions instead of 10 separate tests so the test file stays readable while still exercising every individual branch — each table iteration creates a fresh `createObjectiveFixtureProject`, mutates exactly one field on the base record built by the shared `buildValidObjectiveQueueRecord` helper, writes that corrupted record to `queue.jsonl`, and asserts `readObjectiveQueueRecords` fails closed with the canonical field-specific error message. No runtime change, no new schema, no in-place edit of earlier seq rows; same `W4-QUEUE-AND-TASK-SELECTION` feature id. Out-of-scope follow-ups transparently NOT closed here and unchanged from Round 71: (a) spec plan file 05 line 193 idempotent/resumePolicy branch still requires task-registry schema extension + registry entry for `analysis-execution-run` + runtime wiring, deferred to T4.5+; (b) frozen non-CI sleeping-workstation wake acceptance test still pending, keeps spec-side scheduler cluster row `in-progress`; (c) `archive/vibe-science-v5.5-codex` gitlink warning predates Wave 4. |
| 089 | 2026-04-24 | 4 | W4-BLOCKER-AND-MORNING-DIGEST | Land frozen T4.4 blocker and morning-digest behavior by extracting canonical objective-local writers for `BLOCKER.flag` and immutable digest artifacts, wiring blocker transitions through them, and pinning `objective status` wake-health + morning-review surfaces without using the digest as control state | `environment/objectives/store.js`, `environment/objectives/blocker-flag.js`, `environment/objectives/digest-writer.js`, `environment/orchestrator/autonomy-runtime.js`, `environment/tests/cli/objective-cli.test.js`, `environment/tests/cli/research-loop.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/cli/objective-cli.test.js` (11/11 pass, including the new morning-review status regression that pins `wakePolicy.lastWakeId`, `leaseStatus`, `currentWakeLease`, `latestDigestExists`, the newest immutable digest path, and capability-summary visibility while proving `digest-latest.md` matches the newest immutable digest and contains no unbound closure-claim phrasing); `node --test environment/tests/cli/research-loop.test.js` (22/22 pass, now pinning that the unattended empty-queue / 3am blocker path writes `BLOCKER.flag`, returns a digest path, writes immutable `digest-<ts>.md` plus `digest-latest.md`, and includes snapshot / event log / handoff ledger / queue pointers in the morning digest); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass, including the new `blocker-flag` and `objective-digest` surfaces); `npm run build:surface-index` (42 total surfaces, including `blocker-flag` and `objective-digest` under `W4-BLOCKER-AND-MORNING-DIGEST`); `npm run test:phase9` (250 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators) | verified | Round 73 lands frozen T4.4 after adversarial re-review of the post-Round-72 state. Real drift found and closed in the same pass: the candidate-block branch in `autonomy-runtime.js` (`E_LLM_REASONING_REQUIRED` / morning blocker flow when no sanctioned next slice exists) routed through `blockResearchLoop(...)` without `writeDigest: true`, so the runtime could write `BLOCKER.flag` yet omit the immutable morning digest and `payload.digestPath`. Closure is deliberately narrow and plan-faithful: `environment/objectives/blocker-flag.js` is now the canonical atomic writer for `BLOCKER.flag`; `environment/objectives/digest-writer.js` is the canonical immutable digest writer and now renders explicit `Snapshot Path`, `Event Log Path`, `Handoff Ledger Path`, and `Queue Path` lines; `environment/objectives/store.js` exports the reviewed `atomicWriteUtf8` helper so both new writers stay on the existing atomic-write primitive; and `autonomy-runtime.js` routes both completed-slice and blocker flows through the new writers while keeping `digest-latest.md` a convenience copy, NOT control state. The strengthened `objective-cli.test.js` pins the T4.4 `objective status` contract honestly: the CLI surfaces wake health and the latest morning-review artifact, but intentionally reports the newest immutable digest when immutable digests exist rather than forcing the convenience alias path. No new schema, no cross-wave scope creep, and no in-place edit of earlier seq rows. Honest residuals remain outside T4.4: (a) the sleeping-workstation manual wake acceptance test for the scheduler cluster is still pending, so Wave 4 scheduler rows remain `in-progress`; (b) the broader immutable acceptance-bundle contract owned by file 08 row 105 is still later-wave work; (c) the pre-existing `archive/vibe-science-v5.5-codex` gitlink warning in vibe-science CI predates Wave 4. |
| 090 | 2026-04-24 | 4 | W4-BLOCKER-AND-MORNING-DIGEST | Close the residual T4.4 drift that seq 089 missed: `applyBudgetStopCondition` blocked branch (autonomy-runtime.js:685) and `handleSemanticDrift` (autonomy-runtime.js:747) wrote `BLOCKER.flag` via the canonical `writeObjectiveBlockerFlag` but bypassed the canonical `writeObjectiveDigest`, so two of the three blocker-flag code paths produced NO morning digest and the operator had to reconstruct state from events.jsonl. Seq 089 claimed "wiring blocker transitions through them" but only rewired the `blockResearchLoop(...)` flow (empty-queue / incomplete-at-crash / terminal-without-event blockers). Budget-exhausted block and semantic-drift pause both now route through `writeObjectiveDigest` with full snapshot/events/handoffs/queue pointers, and the CLI payload surfaces `digestPath` so the morning-operator flow is complete for every path that writes `BLOCKER.flag` | `environment/orchestrator/autonomy-runtime.js`, `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/cli/research-loop.test.js` (22/22 pass post-Round-74, same count as Round 73 — added dedicated assertions inside the existing `onBudgetExhausted=block` budget-exhaustion test and the `SEMANTIC_DRIFT_DETECTED` test, pinning `payload.digestPath` match against `/digest-latest\.md$/`, `digestLatestPath` existence on disk, and the four frozen morning-digest pointers (`Snapshot Path`, `Event Log Path`, `Handoff Ledger Path`, `Queue Path`) plus the specific `Digest Kind: budget-exhausted` / `Digest Kind: semantic-drift` strings so a silent regression cannot drop the digest from either path); RED-first proof: replacing the new `writeObjectiveDigest(...)` call at `applyBudgetStopCondition` blocked branch with a stub `{ latestPath: "nowhere" }` triggered the new budget-block regression with `The input did not match the regular expression /digest-latest\.md$/u` before revert, confirming the test pins the exact contract; `npm run test:phase9` (250 pass, 0 fail, 5 skipped, same count as Round 73 — new coverage lives inside existing tests); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` (green on Round 74 delta including spec-side paths) | verified | Round 74 adversarial closure on top of seq 089. Two independent agent reviews of the post-Round-73 state returned different verdicts (one "clean pass", one claimed P0 write-ordering drifts that turned out to be spec-gospel-correct not drift); the REAL drift, invisible to both first-pass reports, was that three distinct code paths write `BLOCKER.flag` but only one also wrote the morning digest — the specific drift was found by enumerating every `writeObjectiveBlockerFlag` call-site and cross-checking each against `writeObjectiveDigest` call-sites in the same function. Seq 089 landed canonical writers correctly but only rewired `blockResearchLoop(...)`; the other two call-sites (budget-exhausted block at line 685, semantic-drift pause at line 747) kept writing only the flag + event + snapshot, so a morning operator waking up to either of these blockers had no canonical digest with the required pointers to snapshot/events/handoffs/queue — contradicting the T4.4 plan-file bullet "Write immutable digest-<ts>.md with pointers to snapshot/events/handoffs/queue" on a blocked transition. Closure is narrow and spec-faithful: both `applyBudgetStopCondition` blocked branch and `handleSemanticDrift` now call `writeObjectiveDigest` with digestKind `budget-exhausted` / `semantic-drift` and surface `digestPath` in the CLI payload. No new surface, no new schema, no in-place edit of seq 089; same `W4-BLOCKER-AND-MORNING-DIGEST` feature id. Out-of-scope follow-ups unchanged: (a) sleeping-workstation manual wake acceptance test still pending; (b) broader immutable acceptance-bundle contract owned by file 08 row 105 still later-wave work; (c) `archive/vibe-science-v5.5-codex` gitlink warning predates Wave 4. |
| 091 | 2026-04-24 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Land the T4.5 runtime-side external wake contract end-to-end: `research-loop` now accepts a reviewed external caller identity through `VRE_EXTERNAL_WAKE_CALLER`, validates it fail-closed, falls heartbeat ownership back to the persisted objective wake policy, records wake caller / wake id durably across wake leases, heartbeat/state-repair/stale/blocker payloads, and no-op heartbeat snapshots, and keeps duplicate/stale wake handling deterministic while surfacing the caller in structured JSON | `bin/vre`, `environment/orchestrator/autonomy-runtime.js`, `environment/schemas/phase9-objective-event.schema.json`, `environment/tests/cli/research-loop.test.js` | `VRE_EXTERNAL_WAKE_CALLER` | `node --test environment/tests/cli/research-loop.test.js` (23/23 pass, including duplicate-wake no-op heartbeat-event + snapshot wake-id recording, wake-lease-still-active no-op heartbeat-event + snapshot wake-id recording, stale-lease reclaim payload wake metadata, crash-repair `state-repair` wake metadata, and an explicit compatibility-adapter heartbeat proving `payload.wakeCaller === "plugin-loop-wake"` and `currentWakeLease.acquiredBy === snapshot.wakeLease.acquiredBy === "plugin-loop-wake"`); `npm run test:phase9` (252 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators); `npm run check` (736 pass, 0 fail, 9 skipped) | verified | Round 75 runtime half of frozen T4.5. Deep review of the post-Round-74 state found two real residual drifts while landing the wake-caller contract: duplicate/contended heartbeat no-op paths wrote only the next snapshot and therefore failed the file-11 contract "Record a unique wakeId in events.jsonl and in the next resume snapshot" / "Write a digest or event after every wake", and the heartbeat probe path still accepted unreviewed caller identities without the same allowlist validation as the real runtime. Closure stays narrow and plan-faithful: `bin/vre` now shares the same reviewed wake-caller validator for both the real heartbeat path and the probe path; `autonomy-runtime.js` appends an explicit `heartbeat` event for duplicate/unexpired-lease no-ops, records the contending `wakeId` / `wakeCaller` into `resume-snapshot.nextAction.params` so the next snapshot still names the rejected wake, threads `wakeId` / `wakeCaller` through blocker / budget / semantic-drift / stale-reclaim payloads, and widens the `state-repair` event payload schema just enough to persist recovery wake metadata without inventing a new event kind or schema family. The wake caller remains informational metadata layered on top of the existing objective pointer / wake lease / queue / event-log authorities rather than replacing them. |
| 092 | 2026-04-24 | 4 | W4-SCHEDULER-REGISTRATION-CLI | Land the T4.5 scheduler/plugin wake-caller boundary: the canonical Windows task definition remains `node <vre>/bin/vre research-loop --heartbeat --wake-id auto --json`, the scheduler heartbeat probe now truthfully reports `wakeCaller=windows-task-scheduler`, and the plugin-side compatibility adapter `plugin/scripts/loop-wake.js` shells out to the sibling VRE CLI with `VRE_EXTERNAL_WAKE_CALLER=plugin-loop-wake` instead of embedding wake logic locally | `environment/orchestrator/windows-task-scheduler.js`, `environment/tests/control/windows-task-scheduler.test.js`, `../vibe-science/plugin/scripts/loop-wake.js`, `../vibe-science/tests/phase9-loop-wake.test.mjs`, `../vibe-science/package.json` | `VRE_EXTERNAL_WAKE_CALLER` | `node --test environment/tests/control/windows-task-scheduler.test.js` (18/18 pass, now pinning the canonical `node <vre>/bin/vre ...` argv contract on install, truthful `wakeCaller === "windows-task-scheduler"` on the manual heartbeat-probe path, and fail-closed rejection of an unreviewed probe caller identity); `node --test tests/phase9-loop-wake.test.mjs` in `vibe-science` (4/4 pass, covering sibling VRE discovery, canonical heartbeat argv construction, env injection with `plugin-loop-wake`, required-arg fail-closed behavior, and missing-sibling fail-closed behavior); `npm run test:phase9` in `vibe-science` (26/26 pass); `npm test` in `vibe-science` (green: 171 E2E + 342 Phase 8 + 26 Phase 9 pass) | verified | Round 75 scheduler/plugin half of frozen T4.5. The canonical Phase 9 v1 wake path remains Windows Task Scheduler invoking the VRE CLI directly; no thread-local timer or scheduler-embedded research logic is introduced here. Closure therefore stays boundary-focused: `windows-task-scheduler.js` only annotates the manual heartbeat probe with the canonical scheduler caller identity, while the new plugin-side `loop-wake.js` is deliberately just a compatibility adapter that discovers the sibling VRE repo, builds the exact reviewed `research-loop --heartbeat --objective ... --wake-id ... --json` invocation, and marks the caller as `plugin-loop-wake`. This satisfies the frozen "scheduler is only a wake caller" rule without promoting the plugin adapter into the v1 acceptance path or turning the scheduler layer into an alternate source of truth. |
| 093 | 2026-04-24 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Round 76 status-ledger metrics refresh closure on top of seq 091 + 092: Codex's Round 75 spec-side refresh only updated rows 83/85/95/96/106 but left rows 70/82/94/97/104/107/109 citing stale metrics (`250 pass, 0 fail, 5 skipped` instead of post-Round-75 `252`; `22/22 pass post-Round-71` / `22/22 pass post-Round-74` / `22/22 pass` instead of `23/23 pass post-Round-75`; `npm test` 734/734 instead of 736/736 in the Phase 8 meta-cluster). This is the same half-update pattern that Round 52 (seq 067), Round 66 tail, and Round 74 (seq 090 row 104/107 citation fix) have had to catch. Round 76 refreshes the 7 affected spec-side rows via deterministic sed propagation so no cluster citing the VRE test:phase9 count carries a stale number after Round 75 has landed | `phase9-vre-feature-ledger.md` | none | `grep -c '252 pass, 0 fail, 5 skipped' blueprints/private/phase9-vre-autonomous-research-loop/16-implementation-status-ledger.md` (11 rows now cite the refreshed count); `grep -c '250 pass, 0 fail, 5 skipped'` (0 residuals); `grep -c '23/23 pass post-Round-75'` (6 rows cite the refreshed research-loop test count); `grep -c '22/22 pass'` (0 residuals); `grep -c '736/736 pass'` on row 70 Phase 8 meta-cluster confirms the npm test count refresh; `npm run test:phase9` (252 pass, 0 fail, 5 skipped — unchanged from Round 75 baseline because this is pure tracking hygiene with no code changes); `npm run validate` (green, all 12 validators); explicit `npm run check:phase9-ledger -- --changed-file=...` green on Round 76 delta | verified | Round 76 is documentary closure only — no runtime code changed. Follows the pattern established by seq 067 (Round 52 row 85 stale-metrics closure), where a spec-side half-update was caught and surfaced via a dedicated VRE ledger row so the rolling delta stays auditable. Rows touched: 70 (Phase 8 meta-cluster test:phase9 + npm test counts), 82 (objective lifecycle CLI test:phase9), 83 (already refreshed by Round 75 — confirmed), 94 (rule-only unattended loop research-loop count), 97 (strategic semantic drift research-loop count), 104 (acceptance evidence research-loop count), 107 (morning artifacts research-loop count), 109 (schemas/fixtures research-loop count). Out-of-scope residuals explicitly NOT closed here: per-row `seq 091` citation addenda for rows 97/104/107/109 (tracked as "nice-to-have narrative detail" rather than metric drift; content of those rows still accurately describes the behaviors even without the seq 091 anchor, and the primary half-update was the test-count propagation). Transparent follow-up: row 71 `vibe-science test:phase9 (22/22 pass)` was NOT updated to `26/26` because the column of row 71 is dated `2026-04-23` (Round 58) and pre-dates the Round 75 landing; historical rows use their landing-date counts by design. Same stopping criterion applies: after Round 76 every cluster that cites the current test:phase9 count is coherent with the Round 75 baseline, and no row carries a Round-71/Round-74 stale number. |
| 094 | 2026-04-24 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Land frozen T4.6 strategic semantic drift checkpoint end-to-end: unattended `research-loop` now defaults to a reviewed deterministic scorer backed by the new `semantic-drift-checkpoint` orchestrator surface, `uncertain` pauses for review instead of silently continuing, final-quarter drift payloads keep the correct phase, the semantic-drift event schema/fixtures are pinned to the live payload shape, and the machine surface inventory grows by one surface | `environment/orchestrator/autonomy-runtime.js`, `environment/orchestrator/semantic-drift-checkpoint.js`, `environment/schemas/phase9-objective-event.schema.json`, `environment/tests/fixtures/phase9/objective-event/valid-semantic-drift-detected.json`, `environment/tests/fixtures/phase9/objective-event/invalid-semantic-drift-missing-phase.json`, `environment/tests/schemas/phase9-objective-event.schema.test.js`, `environment/tests/cli/research-loop.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/cli/research-loop.test.js` (28/28 pass, including deterministic aligned/drifted/uncertain scorer coverage, tangential handoff drift, `uncertain` pause-for-review without `BLOCKER.flag`, no-drift continuation on the bounded-slice happy path, and correct `payload.phase` for both pre-slice and final-quarter strategic drifts); `node --test environment/tests/schemas/phase9-objective-event.schema.test.js` (6/6 pass, pinning the live `semantic-drift-detected` payload shape plus missing-phase rejection); `node --test environment/tests/schemas/phase9-*.test.js` (56/56 pass); `node --test environment/tests/ci/phase9-surface-index.test.js` (2/2 pass); `node --test environment/tests/cli/*.test.js` (78 pass, 0 fail, 4 skipped on the local Windows host); `npm run build:surface-index` (43 total surfaces, now including `semantic-drift-checkpoint`); `npm run test:phase9` (258 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators); `npm run check` (742 pass, 0 fail, 9 skipped) | verified | Round 77 lands frozen T4.6 after adversarial re-review found that the pre-existing strategic checkpoint was only a stub defaulting to `aligned`, handled `drifted` but not `uncertain`, and could stamp final-quarter semantic-drift events as `pre-slice` because the phase lived only in the callback input, not the returned verdict. Closure stays narrow and spec-faithful: new `environment/orchestrator/semantic-drift-checkpoint.js` deterministically reads objective question/current stage/recent queue+event evidence/recent handoffs/open blockers/budget remaining and returns `aligned | uncertain | drifted` without inline LLM adjudication; `research-loop` evaluates it before every unattended slice and again before the final budget tranche; tangential evidence pauses with `SEMANTIC_DRIFT_DETECTED`; weak-overlap `uncertain` pauses the objective with a review-needed resume snapshot + `r2-request` event but deliberately does NOT fabricate a reviewer-2 handoff or blocked-state `BLOCKER.flag`, because row 93 reviewer-2 bridge semantics remain `not-started`; and the semantic-drift event schema now pins `{ code, message, phase, wakeId?, wakeCaller? }` with a matching negative fixture. Same-pass documentary closure also corrects the spec-side Round-75 invocation note so the canonical scheduler command remains objective-bearing: `node <vre>/bin/vre research-loop --objective <OBJ-ID> --heartbeat --wake-id auto --json`. |
| 095 | 2026-04-24 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Round 78 seq-094 claim-without-pin closure on top of T4.6 landing: deep adversarial enumeration of seq 094 found four real residual drifts that the first-pass review reported clean. (1) `autonomy-runtime.js:804` `STRATEGIC_CHECKPOINT_VERDICTS` defensive validator rejects out-of-allowlist verdicts into a safe `uncertain` fallback but had no dedicated test — a silent relaxation of the allowlist would otherwise regress into slice execution. (2) The runtime dispatches both the pre-slice and the final-quarter strategic checkpoint to `handleStrategicUncertain`, but only the pre-slice path was pinned end-to-end — the final-quarter-uncertain dispatch and phase threading through `normalizeCheckpointResult` were a symmetric gap relative to the already-pinned final-quarter-drifted case. (3) `handleStrategicUncertain` already writes `checkpoint: 'uncertain'` + `requestedReviewer: 'reviewer-2-or-lead'` into the `r2-request` payload to communicate the spec file-11 line-222 "request Reviewer-2 or lead review" requirement, but the existing test only pinned `code` / `phase` / `wakeId` / `wakeCaller`, so the reviewer target + checkpoint source were silently drop-able. (4) `handleSemanticDrift` explicitly passes a synthetic `blocker: { exists: true, code, message, writtenAt }` into `writeRuntimeResumeSnapshot` so the drift-paused snapshot already names the drift as an open blocker before `writeObjectiveBlockerFlag` produces the same truth on disk, but no test pinned that coherence between the snapshot and the BLOCKER.flag — reverting the explicit `blocker` option would silently show the snapshot as `openBlockers: []` while BLOCKER.flag says otherwise. Round 78 is test-only closure: one new unsupported-verdict regression (RED-first verified), one new final-quarter-uncertain regression, and enhanced assertions on the existing uncertain + drift tests. No new runtime surface, no new schema, no in-place edit of seq 094. Same featureId as seq 094 | `environment/tests/cli/research-loop.test.js` | none | `node --test environment/tests/cli/research-loop.test.js` (30/30 pass post-Round-78, adding an unsupported-verdict fallback regression and a final-quarter-uncertain dispatch regression plus pinning `r2-request.payload.checkpoint` + `requestedReviewer` + reviewer-target message shape on the existing pre-slice uncertain test, and pinning `snapshot.openBlockers[0].code === 'SEMANTIC_DRIFT_DETECTED'` on the existing drift test so the snapshot stays coherent with the upcoming BLOCKER.flag); RED-first verified on the unsupported-verdict branch by commenting out the `!STRATEGIC_CHECKPOINT_VERDICTS.has(...)` guard in `autonomy-runtime.js` (new regression fired with `Expected values to be strictly equal: 'slice-complete' !== 'paused'` before revert); `npm run test:phase9` (260 pass, 0 fail, 5 skipped — +2 over the Round-77 258 baseline); `npm run validate` (green, all 12 validators); `npm run check` (744 pass, 0 fail, 9 skipped — +2 over the Round-77 742 baseline); explicit `npm run check:phase9-ledger -- --changed-file=...` green on Round 78 delta | verified | Round 78 is pure test-only discipline on top of seq 094. Follows the same methodology that Rounds 61/62/67/71/72/74 used to close claim-without-pin gaps after a landing round: enumerate every declared behavior in the landing row, locate the dedicated test that pins it, close gaps where no test exists. Stopping criterion: after Round 78 every `aligned | uncertain | drifted` verdict, every pre-slice vs final-quarter dispatch site, every metadata field carried by the `r2-request` payload, and the `snapshot.openBlockers` coherence with the upcoming BLOCKER.flag on the drift path are now directly pinned by `research-loop.test.js`. Transparent out-of-scope follow-up NOT closed here: the schema for `semantic-drift-detected` has only one negative fixture (`invalid-semantic-drift-missing-phase.json`) and omits symmetric negative coverage for `invalid-code-value`, `invalid-phase-enum`, `extra-property`, and `missing-message`; this mirrors the pre-existing `state-repair` fixture pattern (only `invalid-state-repair-missing-repaired-layer.json` exists) and is therefore tracked as consistent-with-precedent rather than a regression, to be closed in a later schema-hardening round alongside the state-repair fixture gap. Wave 4 stays `IN_PROGRESS`: the manual sleeping-workstation wake acceptance test is still pending. |
| 096 | 2026-04-24 | 4 | W4-AUTONOMY-RUNTIME-LOOP | Round 79 scorer-branch coverage closure on top of seq 094 + seq 095: the "sicuro?" adversarial pressure after Round 78 surfaced that the deterministic scorer inside `environment/orchestrator/semantic-drift-checkpoint.js` has 7 distinct decision branches but seq 094 shipped with only 3 direct scorer-level unit tests (the `aligned via overlap` final branch, the `drifted via explicit contradiction` branch, and the `uncertain in final-quarter` branch). Four branches were silently unpinned: (1) `drifted via taskKindsOutsideStage` at line 181, (2) `aligned via vacuous evidence` at line 207 when there are no handoffs + no contradictions + no outside task kinds, (3) `drifted via no-overlap-with-handoffs` at line 220 when handoffs exist but share no tokens with either the objective question or the current stage intent AND there is no explicit contradiction, and (4) `uncertain in pre-slice` at line 249 when overlap is weak but the final-quarter signal has not yet fired. A silent refactor or logic inversion inside the scorer could therefore flip any of these 4 branches into its neighbor (drifted ↔ uncertain ↔ aligned) without any test noticing. Round 79 is test-only closure: 4 new scorer-level unit tests, each exercising exactly one of the remaining branches, plus RED-first verification on the `drifted via no-overlap-with-handoffs` branch by commenting out the `if (objectiveOverlap === 0 && recentHandoffs.length > 0 && stageOverlap === 0) {...}` block (new regression fired with `Expected values to be strictly equal: 'uncertain' !== 'drifted'` before revert, confirming the branch ordering is load-bearing). No runtime change, no new schema, no in-place edit of seq 094 / seq 095. Same featureId as seq 094 / seq 095 | `environment/tests/cli/research-loop.test.js` | none | `node --test --test-name-pattern="deterministic strategic checkpoint" environment/tests/cli/research-loop.test.js` (7/7 pass post-Round-79, covering all 7 scorer decision branches end-to-end); `node --test environment/tests/cli/research-loop.test.js` (34/34 pass post-Round-79); `npm run test:phase9` (264 pass, 0 fail, 5 skipped — +4 over the Round-78 260 baseline from the 4 new scorer branch tests); `npm run validate` (green, all 12 validators); `npm run check` (748 pass, 0 fail, 9 skipped — +4 over the Round-78 744 baseline); explicit `npm run check:phase9-ledger -- --changed-file=...` green on Round 79 delta | verified | Round 79 is pure test-only discipline on top of seq 094 / seq 095. Follows the same "sicuro?" adversarial methodology that has surfaced real drifts in every Round 67 / 68 / 71 / 72 / 74 / 76 / 78 cycle: after a landing round is declared clean, enumerate every decision branch inside every new surface and locate the direct test for each, then close the gap where no direct test exists. Stopping criterion: after Round 79 every one of the 7 scorer decision branches (`drifted-via-task-kind`, `drifted-via-explicit-contradiction`, `aligned-via-vacuous`, `drifted-via-no-overlap-with-handoffs`, `uncertain-in-final-quarter`, `uncertain-in-pre-slice`, `aligned-via-overlap`) is directly pinned by a dedicated scorer-level unit test, plus the 4 runtime-side integration tests added in Round 77 / 78 for the pre-slice + final-quarter × drifted + uncertain dispatch matrix. Transparent out-of-scope NOT closed here: the dead `INTERNALS = Object.freeze({...})` export at line 274 of `semantic-drift-checkpoint.js` is currently unused by any test or runtime caller — removing it is a pure hygiene follow-up that belongs to a later cleanup round, not a drift. Wave 4 stays `IN_PROGRESS`: the manual sleeping-workstation wake acceptance test is still pending. |
| 097 | 2026-04-24 | 4.5 | W45-ROLE-BOUNDED-DISPATCH | Land frozen T4.5.1 role-bounded dispatch: new `agent-orchestration` binds exactly the 7 file-07 roles onto inline-only vs reviewed subprocess lanes, reuses provider-gateway/executor dispatch instead of Task-style background delegation, validates `phase9.role-envelope.v1` plus explicit session/workspace isolation, patches `dispatchParentPid` atomically before reviewed dispatch, blocks non-lead objective mutation and chat-only reviewed subprocess launch, and fail-closes env/cwd/stdio/argv transport leaks | `environment/orchestrator/agent-orchestration.js`, `environment/tests/control/agent-orchestration.test.js`, `environment/tests/ci/phase9-surface-index.js`, `environment/tests/ci/phase9-surface-index.test.js`, `environment/tests/ci/validate-runtime-contracts.js`, `environment/tests/ci/validate-counts.js`, `package.json`, `phase9-vre-surface-index.json` | none | `node --test environment/tests/control/agent-orchestration.test.js` (17/17 pass, including the exact role set, inline-only literature-mode positive path, execution-lane and reviewer-2 reviewed dispatch planning, `execute=true` invokeLaneBinding with a patched envelope, missing objective/envelope/isolation fail-closed, env leak + env allowlist + cwd + stdio + argv fail-closed, unsupported role, non-lead mutation block, web-role Task-style rejection, and chat-only reviewed-dispatch rejection); `node --test environment/tests/control/*.test.js` (146/146 pass); `npm run build:surface-index` (44 total surfaces, now including `agent-orchestration` under `W45-ROLE-BOUNDED-DISPATCH`); `npm run test:phase9` (281 pass, 0 fail, 5 skipped); `npm run validate` (green, all 12 validators); `npm run check` (765 pass, 0 fail, 9 skipped) | verified | Round 80 lands frozen T4.5.1 after direct adversarial enumeration confirmed the last T4.6 pass was clean and then found one more claim-without-pin cluster inside the fresh orchestration landing: the positive inline-only path, the `execute=true` provider-gateway dispatch path, and two severance guard branches (`E_ENV_ALLOWLIST_VIOLATED`, `E_FD_LEAK`) were implemented but unpinned. Closure stays exactly inside file 07 / plan 05a: exact supported roles only, no generic free-form dispatch, no Task-tool background delegation for web-required roles, no chat-only reviewed subprocess launch when durable objective state exists, and no non-lead global objective mutation. Honest residuals NOT closed here: full cold-child startup severance proof (`process.ppid`/live-parent query, TTY/runtime-state/token absence) remains the separate row-92 cluster; handoff persistence and reviewer-2 bridge semantics remain row 93 later-wave scope; the manual sleeping-workstation wake acceptance test remains pending but does not block T4.5.1. |
| 098 | 2026-04-24 | 4.5 | W45-ROLE-BOUNDED-DISPATCH | Round 81 seq-097 claim-without-pin closure on top of T4.5.1 landing: deep adversarial enumeration of every `fail(...)` call and every exported symbol in `environment/orchestrator/agent-orchestration.js` found that seq 097 shipped with only 17 direct tests but had 14 fail-closed error-code branches without dedicated pins, 2 exported functions (`getRoleDispatchContract`, `validateReviewedSpawnRequest`) never called directly from the test suite, and 2 dispatch branches (continuity-agent queue-task-or-inline-only routing based on `taskKind` presence + `dispatchRoleAssignment` with `execute=true` against an inline-only role that must early-return without executing) that were only transitively exercised. A silent refactor or logic inversion inside any of those branches could flip role→task mapping, skip fail-closed validation, or dispatch a non-reviewed child without any test catching it. Round 81 adds 18 new regressions covering every remaining branch: `E_VRE_SURFACE_MISSING` (missing Wave-4.5 provider-gateway surface), `E_ROLE_INLINE_ONLY` requestedMode-mismatch variant, `E_ROLE_INLINE_ONLY` inline-plus-provider-gateway-transport variant, `E_ROLE_DISPATCH_MODE_UNSUPPORTED` queue-task-or-inline-only variant, `E_ROLE_DISPATCH_MODE_UNSUPPORTED` regular mismatch, `E_TASK_KIND_REQUIRED` / `E_TASK_KIND_UNSUPPORTED` / `E_TASK_KIND_NOT_ALLOWED` (RED-first verified) / `E_TASK_KIND_LANE_MISMATCH`, `E_EXECUTOR_CLASS_NOT_ALLOWED` via injected `selectLaneBinding`, `E_TASK_ID_REQUIRED`, `E_SESSION_ISOLATION_REQUIRED` inheritChatHistory-not-false variant, `E_WORKSPACE_WRITE_ESCAPE` scratchRoot-escape, continuity-agent queue-task positive path plus inline-only fallback, `dispatchRoleAssignment` execute=true + inline-role early-return, `getRoleDispatchContract` direct pin (known roles + null for unknown), and `validateReviewedSpawnRequest` direct pin against `E_ENV_LEAK` + `E_ENV_ALLOWLIST_VIOLATED`. Same-pass tracking closure also downgrades spec-side row 91 from `verified` to `in-progress` because spec file-07 §Subprocess Spawn Contract defines "reviewed subprocess transport" to require the actual cold-child spawn + live-parent / `process.ppid` probe + resolved-realpath normalization + `E_INHERITED_SESSION_TOKEN` + `E_ORPHANED_SPAWN_PARENT` + runtime `E_WORKSPACE_WRITE_ESCAPE` IO wrapper; the currently landed code only implements the envelope + pre-dispatch spawn-request validation layer, and the runtime cold-child side remains pending under row 92. No new runtime surface, no new schema, no in-place edit of seq 097. Same featureId as seq 097 | `environment/tests/control/agent-orchestration.test.js` | none | `node --test environment/tests/control/agent-orchestration.test.js` (35/35 pass post-Round-81, +18 over the Round-80 17/17 baseline); RED-first verified on `E_TASK_KIND_NOT_ALLOWED` by commenting out the `if (!roleContract.allowedTaskKinds.includes(taskKind)) {...}` branch in `agent-orchestration.js` (new regression fired with `AssertionError: Missing expected rejection` before revert, confirming the branch is load-bearing for role→task-kind matrix enforcement); `node --test environment/tests/control/*.test.js` (164/164 pass post-Round-81, +18 over the Round-80 146/146 baseline); `npm run test:phase9` (299 pass, 0 fail, 5 skipped — +18 over the Round-80 281 baseline); `npm run validate` (green, all 12 validators); `npm run check` (783 pass, 0 fail, 9 skipped — +18 over the Round-80 765 baseline); explicit `npm run check:phase9-ledger -- --changed-file=...` green on Round 81 delta | verified | Round 81 is pure test-only + tracking-honesty discipline on top of seq 097. Follows the same adversarial methodology used in Rounds 61/62/67/71/72/74/78/79: after a landing round is declared clean, enumerate every fail-closed branch + every exported symbol in the new surface, locate the dedicated test for each, close gaps where none exists, and RED-first verify a load-bearing sample. The row-91 downgrade is the honest consequence of reading spec file-07 as gospel: the spec defines "reviewed subprocess transport" as the full cold-child spawn contract (lines 37-98), not as envelope generation plus pre-dispatch validation, so `verified` was overclaimed even though 17/17 tests were green on the code actually written. Transparent out-of-scope NOT closed here: the cold-child runtime side (`process.ppid` probe, `E_ORPHANED_SPAWN_PARENT`, `E_INHERITED_SESSION_TOKEN`, resolved-realpath normalization via `fs.realpath`, runtime write-sandbox wrapper enforcing `E_WORKSPACE_WRITE_ESCAPE`) requires landing a child-side runner plus the reviewed IO wrapper and belongs to T4.5.2+ rather than T4.5.1. Wave 4 + Wave 4.5 stay `IN_PROGRESS`: the manual sleeping-workstation wake acceptance test (row 95-96 context) is still pending, and the cold-child severance runtime side (row 92) is not yet landed. |
| 099 | 2026-04-24 | 4.5 | W45-ROLE-BOUNDED-DISPATCH | Round 82 seq-098 residual claim-without-pin closure on top of T4.5.1 landing + Round 81 pinning: "sicuro?" second-pass adversarial enumeration on the exact line numbers where `agent-orchestration.js` raises each error code found 2 more requireString-guarded call sites without dedicated tests that Round 81's 18 pins did not reach. (1) Line 324 `requireString(isolation.workspaceRoot, ..., 'E_SESSION_ISOLATION_REQUIRED')` — the existing `E_SESSION_ISOLATION_REQUIRED` tests fire the line-308 isolation-null variant and the line-316 inheritChatHistory variant, but the case where `sessionIsolation` is an object with `inheritChatHistory: false` and NO `workspaceRoot` hits line 324 and was unpinned. (2) Line 344 `requireString(isolation.scratchRoot, ..., 'E_SESSION_ISOLATION_REQUIRED')` when the field is explicitly provided as a non-string (e.g. a number) — Round 81's scratchRoot-escape test provides a valid string that escapes, hitting line 346 `E_WORKSPACE_WRITE_ESCAPE`, which is a different branch. Round 82 is test-only: 2 new regressions close both gaps, and RED-first verified on the line-324 workspaceRoot branch by replacing the `requireString` call with `isolation.workspaceRoot ?? projectRoot` (new regression fired with `AssertionError: Missing expected rejection` before revert). Transparent methodology note: a third candidate — the line-518 `requireString` for an empty/missing `roleId` — was investigated and CONSCIOUSLY NOT added here, because an empty-string roleId lands on the line-521 matrix-miss guard with the same `E_UNSUPPORTED_ROLE` code, so the line-518 `requireString` is defensively redundant rather than a distinct branch with observable behavior. No new runtime surface, no new schema, no in-place edit of seq 097 / seq 098. Same featureId as seq 097 / seq 098 | `environment/tests/control/agent-orchestration.test.js` | none | `node --test environment/tests/control/agent-orchestration.test.js` (37/37 pass post-Round-82, +2 over the Round-81 35/35 baseline); RED-first verified on the line-324 workspaceRoot branch; `npm run test:phase9` (301 pass, 0 fail, 5 skipped — +2 over the Round-81 299 baseline); `npm run validate` (green, all 12 validators); `npm run check` (785 pass, 0 fail, 9 skipped — +2 over the Round-81 783 baseline); explicit `npm run check:phase9-ledger -- --changed-file=...` green on Round 82 delta | verified | Round 82 is pure test-only + methodological honesty discipline on top of seq 098. Follows the established "sicuro?" pattern used in every prior round: after a landing+pin round is declared clean, a second direct enumeration by line number frequently finds 2-4 residual requireString-guarded paths that the first pass conflated with adjacent branches of the same error code. Stopping criterion: after Round 82 every observable fail-closed branch of `agent-orchestration.js` is directly pinned by a dedicated test; the only remaining `fail` call without its own test is the defensively redundant line-518 `requireString` for `roleId`, which was enumerated, investigated, and transparently excluded above. Wave 4.5 stays `IN_PROGRESS`: the cold-child runtime side (row 92 axes — `process.ppid` probe, `E_ORPHANED_SPAWN_PARENT`, `E_INHERITED_SESSION_TOKEN`, `fs.realpath`, runtime IO wrapper) still requires a child-side runner + reviewed IO wrapper and belongs to T4.5.2+. Manual sleeping-workstation wake acceptance test still pending. |
| 100 | 2026-04-24 | 4.5 | W45-ROLE-BOUNDED-DISPATCH | Round 83 seq-098/099 data-integrity repair: "sicuro?" third-pass audit on top of Round 82 enumerated ALL featureId columns across the phase9-vre-feature-ledger and cross-referenced against `phase9-vre-surface-index.json`. Found a real featureId drift introduced by my own Round 81 + Round 82 passes: seq 097 (Codex's T4.5.1 landing) correctly used `W45-ROLE-BOUNDED-DISPATCH` matching `phase9-vre-surface-index.json` entry for `agent-orchestration`, but seq 098 (my Round 81 pin closure) and seq 099 (my Round 82 pin closure) used `W45-AGENT-ORCHESTRATION` — a synonymous but inconsistent tag I coined without checking the existing inventory. The explicit `check:phase9-ledger` passed green both times because the checker walks by path (surface-path ↔ ledger-path), not by featureId string equality. Same narrative drift propagated into the spec-side logs (11-plan-self-review-log.md Round 81 + Round 82 appends; 14-freeze-manifest.md Round 81 + Round 82 addenda; 12-spec-self-review-log.md Round 81 + Round 82 appends) plus one `Closure (seq 098, W45-AGENT-ORCHESTRATION)` and one `Closure (seq 099, W45-AGENT-ORCHESTRATION)` line each. Round 83 is pure documentary integrity: in-place repair of the featureId column on seq 098 and seq 099 rows back to `W45-ROLE-BOUNDED-DISPATCH` (same pattern as seq 076 Round 63 malformed-markdown repair — historical substance unchanged, only the inconsistent data column is reconciled) + `sed -i 's/W45-AGENT-ORCHESTRATION/W45-ROLE-BOUNDED-DISPATCH/g'` across the 3 spec-side log files so every reference in the frozen state now uses the single canonical tag. No test change, no runtime change, no schema change. Same featureId as seq 097 / seq 098 / seq 099 — precisely the point of the repair | `phase9-vre-feature-ledger.md` | none | `grep -c '\| W45-AGENT-ORCHESTRATION \|' phase9-vre-feature-ledger.md` (0 residuals post-Round-83 — was 2 pre-repair); `grep -c '\| W45-ROLE-BOUNDED-DISPATCH \|' phase9-vre-feature-ledger.md` (3 rows: seq 097 + seq 098 + seq 099); `grep -rc 'W45-AGENT-ORCHESTRATION' blueprints/private/` in sibling vibe-science (0 residuals across all 3 spec-side log files); `phase9-vre-surface-index.json` `agent-orchestration` entry still has `featureId: "W45-ROLE-BOUNDED-DISPATCH"` (unchanged, now consistent with every ledger row instead of only seq 097); `npm run test:phase9` (301 pass, 0 fail, 5 skipped — unchanged, no test change); `npm run validate` (green, all 12 validators); `npm run check` (785 pass, 0 fail, 9 skipped — unchanged); explicit `npm run check:phase9-ledger -- --changed-file=...` green on Round 83 delta | verified | Round 83 is pure data-integrity repair on top of Round 81 + Round 82. Follows the documentary-integrity exception precedent that seq 076 (Round 63 malformed-markdown repair) established for same-pass consistency correction without violating append-only discipline: the row's historical substance is unchanged — seq 098 still closes the Round-81 claim-without-pin gap with the 18 regressions, seq 099 still closes the Round-82 requireString gap with 2 regressions — only the featureId column is reconciled to match the single canonical tag `W45-ROLE-BOUNDED-DISPATCH` used by seq 097 and by the surface index. Stopping criterion reached for T4.5.1 data consistency: after Round 83 every featureId reference for the `agent-orchestration` surface uses the single canonical tag across the VRE ledger, the VRE surface index, and all 3 spec-side log files. Transparent out-of-scope NOT closed here: the row 70 Wave-0 tracking narrative still contains a historical reference to "seq 099" as a Round 30 hypothetical attack example — the hypothetical seq number (from a 2026-04-22 era attack retest) now collides with the real seq 099 (Round 82). This is a readability concern, not a functional drift: the narrative is clearly inside a "Round 30 live attack on real ledger" clause referring to a past hypothetical test, and the checker does not compare narratives for numeric collisions. Leaving the historical narrative untouched preserves the original test evidence. Wave 4.5 stays `IN_PROGRESS`: cold-child runtime side (T4.5.2+) still pending. |
